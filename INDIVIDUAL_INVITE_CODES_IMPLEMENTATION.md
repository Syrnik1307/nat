# 📧 Система индивидуальных инвайт-кодов (Individual Invite Codes)

## Описание

Реализована новая система индивидуальных инвайт-кодов, которая позволяет учителям приглашать учеников на отдельные предметы без создания групп. Ученик может получать приглашения от одного учителя на несколько предметов, просто присоединяясь через инвайт-коды.

## Возможности

### Для учителя:
1. ✅ Создание уникальных 8-символьных инвайт-кодов для каждого предмета
2. ✅ Просмотр списка всех созданных кодов (активные и использованные)
3. ✅ Просмотр кто использовал код и когда
4. ✅ Регенерация кодов (создание нового кода вместо старого)
5. ✅ Удаление неиспользованных кодов
6. ✅ Копирование кода и ссылки для приглашения

### Для ученика:
1. ✅ Присоединение к предметам учителя по коду
2. ✅ Просмотр предмета перед присоединением
3. ✅ Один код используется только один раз (однократное использование)
4. ✅ Быстрое присоединение к новым предметам

## Backend изменения

### Новые модели (schedule/models.py):
```python
class IndividualInviteCode(models.Model):
    teacher = ForeignKey(CustomUser)
    subject = CharField()
    invite_code = CharField(unique=True, max_length=8)
    is_used = BooleanField(default=False)
    used_by = ForeignKey(CustomUser, null=True, blank=True)
    used_at = DateTimeField(null=True, blank=True)
    created_at = DateTimeField(auto_now_add=True)
```

### Новый ViewSet (schedule/views.py):
- `IndividualInviteCodeViewSet` с методами:
  - `create()` - создание кода
  - `destroy()` - удаление кода
  - `regenerate()` - регенерация кода
  - `preview_by_code()` - просмотр информации о коде
  - `join_by_code()` - присоединение по коду

### Новый сериализатор (schedule/serializers.py):
- `IndividualInviteCodeSerializer` - сериализация кодов с информацией о преподавателе и ученике

### Маршруты (teaching_panel/urls.py):
- `/api/individual-invite-codes/` - CRUD операции
- `/api/individual-invite-codes/regenerate/` - регенерация
- `/api/individual-invite-codes/preview_by_code/` - просмотр
- `/api/individual-invite-codes/join_by_code/` - присоединение

## Frontend изменения

### Новые компоненты:
1. **IndividualInvitesManage.js** - страница управления индивидуальными кодами (для учителя)
   - Создание новых кодов
   - Просмотр списка кодов (активные/использованные)
   - Регенерация кодов
   - Удаление неиспользованных кодов
   - Статистика по кодам

2. **IndividualInviteModal.js** - модальное окно для показа кода и ссылки приглашения
   - Отображение кода крупно
   - Копирование кода в буфер обмена
   - Копирование ссылки приглашения
   - Регенерация кода

3. **JoinIndividualModal.js** - модальное окно для присоединения ученика
   - Ввод кода приглашения
   - Валидация кода (8 символов)
   - Автоматический uppercase
   - Обработка ошибок

### Стили:
- **IndividualInvitesManage.css** - стили для страницы управления кодами

### API методы (apiService.js):
```javascript
getIndividualInviteCodes()
createIndividualInviteCode(data)
deleteIndividualInviteCode(id)
regenerateIndividualInviteCode(id)
getIndividualInviteCodeByCode(code)
joinIndividualByCode(code)
```

## Использование

### Учитель создает код:
1. Открывает страницу управления индивидуальными приглашениями
2. Вводит название предмета (например "Математика")
3. Нажимает "Создать код"
4. Система генерирует уникальный 8-символьный код
5. Учитель может скопировать код или ссылку и отправить ученику

### Ученик присоединяется:
1. Получает код или ссылку от учителя
2. Открывает приложение и вводит код
3. Получает подтверждение что присоединился к предмету учителя
4. Может присоединиться к другим предметам того же учителя повторив процесс

## Миграции

Создана миграция `schedule/migrations/0014_individualinvitecode.py` которая:
- Создает таблицу `schedule_individualinvitecode`
- Добавляет индексы для быстрого поиска
- Устанавливает связи с пользователями и группами

Для применения на сервере:
```bash
python manage.py migrate schedule
```

## Тестирование

Создан тест-скрипт `test_individual_invite_codes.py` который проверяет:
1. ✅ Создание учителя и ученика
2. ✅ Создание индивидуального кода
3. ✅ Получение списка кодов
4. ✅ Просмотр информации о коде
5. ✅ Присоединение ученика по коду
6. ✅ Проверка что код помечен как использованный
7. ✅ Попытка использовать код дважды (ошибка)
8. ✅ Регенерация кода
9. ✅ Удаление кодов

## Безопасность

- ✅ Только учителя могут создавать коды
- ✅ Только ученики могут присоединяться
- ✅ Коды уникальны и невозможно предугадать
- ✅ Код работает только один раз
- ✅ Реализованы проверки прав доступа на всех уровнях

## Интеграция с существующей системой

### Совместимость с системой групп:
- Индивидуальные коды работают параллельно с групповыми кодами
- Ученик может быть как в группе, так и подписан через индивидуальный код
- Уникальность кодов проверяется среди обоих типов

### API структура:
- Использует стандартные ViewSet и Serializers DRF
- Следует существующим паттернам Permission Classes
- Интегрирована с TokenAuthentication

## Деплой

Для деплоя на продакшн:

1. **Коммит и пуш:**
```bash
git add -A
git commit -m "feat: Individual invite codes system for one-on-one student enrollment"
git push origin main
```

2. **На сервере (SSH без пароля):**
```bash
ssh tp "cd /var/www/teaching_panel && \
sudo -u www-data git pull origin main && \
cd teaching_panel && \
source ../venv/bin/activate && \
pip install -r requirements.txt && \
python manage.py migrate && \
python manage.py collectstatic --noinput && \
sudo systemctl restart teaching_panel nginx && \
echo '✅ Деплой завершен!'"
```

## Дополнительно

### Возможные улучшения в будущем:
- [ ] Добавить срок действия кода (expires_at)
- [ ] Лимит использования кода (может быть один раз или несколько)
- [ ] Отправка приглашений по email
- [ ] Планирование автоматического отправления кодов
- [ ] Интеграция с CRM системами
- [ ] Analytics по кодам (статистика использования)

### Документация:
- API документация: `/api/` (Swagger/ReDoc)
- Backend test: `teaching_panel/test_individual_invite_codes.py`
- Frontend components: `frontend/src/components/Individual*.js`
