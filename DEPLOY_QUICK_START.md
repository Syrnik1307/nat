# 🚀 Быстрый деплой на сервер

## ✅ Что исправлено

### 1. **NavBarNew.js** - Ошибка загрузки сообщений
**Проблема:** API возвращал HTML вместо JSON (статус 404)
**Решение:** 
- Добавлена проверка статуса ответа (`response.ok`)
- Добавлена проверка Content-Type
- При ошибке устанавливается пустой массив

### 2. **TeacherHomePage.js** - Ошибка `undefined.length`
**Проблема:** `breakdown.groups` и `breakdown.students` могли быть `undefined`
**Решение:**
- Добавлены проверки `breakdown?.groups` перед использованием
- Используется optional chaining (`?.`)

## 🎯 Деплой на сервер

### Простой способ - используйте скрипт!

```powershell
cd c:\Users\User\Desktop\nat
.\deploy_to_server.ps1
```

Скрипт предложит меню:
1. **Полный деплой** - обновит всё (код, зависимости, миграции, фронтенд)
2. **Только бэкенд** - обновит Django без пересборки фронтенда
3. **Только фронтенд** - пересоберёт React
4. **Перезапуск** - перезапустит все сервисы
5. **Миграции БД** - применит миграции
6. **Логи** - покажет логи сервисов
7. **Статус** - статус всех сервисов

### Ручной деплой

1. **Закоммитить изменения:**
```powershell
cd c:\Users\User\Desktop\nat
git add .
git commit -m "fix: исправлены ошибки загрузки ЛК учителя"
git push origin main
```

2. **Подключиться к серверу:**
```powershell
ssh root@72.56.81.163
```

3. **На сервере выполнить:**
```bash
cd /var/www/teaching_panel

# Обновить код
git pull origin main

# Обновить зависимости
source venv/bin/activate
pip install -r teaching_panel/requirements-production.txt

# Применить миграции
python teaching_panel/manage.py migrate

# Собрать статику
python teaching_panel/manage.py collectstatic --noinput

# Собрать фронтенд
cd frontend
npm install
npm run build
cd ..

# Перезапустить сервисы
sudo systemctl restart teaching_panel celery celery-beat nginx
```

4. **Проверить статус:**
```bash
sudo systemctl status teaching_panel
```

5. **Посмотреть логи (если нужно):**
```bash
# Django
sudo journalctl -u teaching_panel -f

# Nginx
sudo tail -f /var/log/nginx/teaching_panel_error.log
```

## 🔍 Проверка после деплоя

1. Откройте: `http://72.56.81.163` (или ваш домен)
2. Войдите как преподаватель
3. Проверьте, что главная страница загружается без ошибок
4. Откройте консоль браузера (F12) - не должно быть красных ошибок

## ❓ Если что-то не работает

### Ошибка 502 Bad Gateway
```bash
# Проверить статус Django
sudo systemctl status teaching_panel

# Посмотреть логи
sudo journalctl -u teaching_panel -n 50
```

### Статика не загружается
```bash
# Пересобрать статику
cd /var/www/teaching_panel
source venv/bin/activate
python teaching_panel/manage.py collectstatic --noinput

# Перезапустить nginx
sudo systemctl restart nginx
```

### Фронтенд показывает старую версию
```bash
# Очистить кэш браузера (Ctrl+Shift+Delete)
# Или жёсткое обновление (Ctrl+F5)

# На сервере пересобрать фронтенд
cd /var/www/teaching_panel/frontend
rm -rf build node_modules
npm install
npm run build
```

### База данных не отвечает
```bash
# Проверить PostgreSQL
sudo systemctl status postgresql

# Проверить подключение
sudo -u postgres psql -l
```

## 📝 Полезные команды

```bash
# Посмотреть все сервисы
sudo systemctl status teaching_panel celery nginx redis-server

# Перезапустить всё
sudo systemctl restart teaching_panel celery celery-beat nginx

# Посмотреть логи Django
sudo journalctl -u teaching_panel -f

# Посмотреть логи Celery
sudo journalctl -u celery -f

# Посмотреть логи Nginx
sudo tail -f /var/log/nginx/teaching_panel_access.log
sudo tail -f /var/log/nginx/teaching_panel_error.log

# Проверить использование памяти
free -h

# Проверить использование диска
df -h
```

## 🎉 Готово!

Теперь ваш сайт обновлён и работает без ошибок!

**Адрес сайта:** http://72.56.81.163

**Админка:** http://72.56.81.163/admin

---

**Дополнительная документация:**
- `DEPLOYMENT_GUIDE.md` - полное руководство по деплою
- `AUTO_DEPLOY_GUIDE.md` - автоматический деплой-скрипт
- `PRE_DEPLOYMENT_CHECKLIST.md` - чеклист перед деплоем
- `PRODUCTION_CHECKLIST.md` - production настройки
