# AI-проверка домашних заданий

## Обзор

Teaching Panel LMS теперь поддерживает автоматическую проверку **текстовых ответов** студентов с помощью AI. Преподаватель может включить эту функцию при создании ДЗ.

## Поддерживаемые AI-провайдеры

### 1. DeepSeek (рекомендуется) ⭐
- **Стоимость**: Очень дешёвый (~$0.001 за запрос)
- **Качество**: Отличное, сравнимо с GPT-4
- **Бесплатные кредиты**: Да, при регистрации
- **Регистрация**: https://platform.deepseek.com/

### 2. OpenAI GPT-4o-mini
- **Стоимость**: ~$0.0015 за запрос
- **Качество**: Хорошее
- **Регистрация**: https://platform.openai.com/

## Настройка

### 1. Получите API ключ

**DeepSeek:**
1. Зарегистрируйтесь на https://platform.deepseek.com/
2. Перейдите в API Keys → Create new API key
3. Скопируйте ключ (начинается с `sk-`)

**OpenAI:**
1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в API keys → Create new secret key
3. Скопируйте ключ

### 2. Добавьте ключ в переменные окружения

**Для разработки** (`.env` файл в `teaching_panel/`):
```bash
DEEPSEEK_API_KEY=sk-ваш-ключ-deepseek
# или
OPENAI_API_KEY=sk-ваш-ключ-openai
```

**Для продакшена** (на сервере):
```bash
export DEEPSEEK_API_KEY=sk-ваш-ключ-deepseek
```

### 3. Примените миграции

```bash
cd teaching_panel
python manage.py migrate
```

### 4. Установите зависимости

```bash
pip install httpx>=0.27.0
```

## Использование

### Для преподавателя

1. **Создайте новое ДЗ** в конструкторе
2. **Включите тумблер** "🤖 AI-проверка текстовых ответов"
3. **Выберите провайдера** (DeepSeek рекомендуется)
4. **(Опционально)** Добавьте контекст для AI:
   - Тема урока
   - Критерии оценки
   - Ожидаемый стиль ответа
5. **Опубликуйте ДЗ**

### Как это работает

1. Студент отправляет текстовый ответ
2. AI анализирует ответ с учётом:
   - Текста вопроса
   - Эталонного ответа (если задан в `config.correctAnswer`)
   - Контекста от преподавателя
   - Максимального балла за вопрос
3. AI выставляет предварительную оценку и пишет комментарий
4. Если AI не уверен (confidence < 70%), ответ помечается для ручной проверки
5. Преподаватель **всегда может изменить** оценку AI

### Пример комментария AI

```
[AI оценка, уверенность: 85%] Ответ в целом верный, студент правильно 
использовал Present Perfect для описания опыта. Незначительная ошибка 
в согласовании времён в последнем предложении.
```

## Технические детали

### Модель Homework

Новые поля:
- `ai_grading_enabled` (bool) - включена ли AI проверка
- `ai_provider` (str) - `'deepseek'` | `'openai'`
- `ai_grading_prompt` (text) - дополнительный контекст для AI

### API

При создании/обновлении ДЗ:
```json
{
  "title": "Present Perfect Practice",
  "ai_grading_enabled": true,
  "ai_provider": "deepseek",
  "ai_grading_prompt": "Тема: Present Perfect. Оценивай грамматику и использование времён.",
  "questions": [...]
}
```

### Ответ AI сохраняется в Answer

- `auto_score` - оценка от AI
- `teacher_feedback` - содержит комментарий AI с префиксом `[AI оценка, уверенность: X%]`
- `needs_manual_review` - true если confidence < 70% или произошла ошибка

## FAQ

**Q: Что если API недоступен?**
A: Ответ помечается для ручной проверки, преподаватель увидит `[AI недоступен]`.

**Q: AI поставил неправильную оценку**
A: Преподаватель может изменить `teacher_score`, который имеет приоритет над `auto_score`.

**Q: Какие типы вопросов проверяет AI?**
A: Только `TEXT` (текстовые ответы). Остальные типы проверяются алгоритмически.

**Q: Сколько это стоит?**
A: DeepSeek: ~$1 на 1000 проверок. OpenAI: ~$1.50 на 1000 проверок.

## Troubleshooting

### "AI проверка недоступна: не настроен API ключ"
→ Проверьте переменную окружения `DEEPSEEK_API_KEY` или `OPENAI_API_KEY`

### Timeout ошибки
→ Увеличьте таймаут в `ai_grading_service.py` (по умолчанию 30 сек)

### Некорректный JSON от AI
→ Логируется как warning, ответ помечается для ручной проверки

---

**Последнее обновление**: Декабрь 2025

---

# 📊 AI-отчёты по студентам

## Обзор

Помимо проверки ответов, AI может анализировать **паттерны ошибок** каждого студента и генерировать персональные отчёты для преподавателя.

## Как использовать

### 1. Откройте раздел AI-отчётов

В меню преподавателя: **🤖 AI-отчёты**

URL: `/teacher/ai-reports`

### 2. Выберите группу

Выберите группу студентов из выпадающего списка.

### 3. Сгенерируйте отчёты

Нажмите **"Сгенерировать отчёты"** — AI проанализирует:
- Все выполненные ДЗ за последние 30 дней
- Оценки и комментарии
- Типы ошибок
- Динамику прогресса

### 4. Изучите результаты

Для каждого студента AI покажет:

**📊 Сводку:**
- Средний балл
- Количество сданных ДЗ
- Тренд прогресса (📈/➡️/📉)

**✅ Сильные стороны:**
- Что получается хорошо

**⚠️ Слабые стороны:**
- Над чем нужно работать

**🔍 Типичные ошибки:**
- Повторяющиеся паттерны ошибок
- Частота каждой ошибки

**💡 Рекомендации:**
- Конкретные советы для улучшения

## API Endpoints

```
GET  /api/analytics/ai-reports/                    - список отчётов
GET  /api/analytics/ai-reports/{id}/               - детали отчёта
POST /api/analytics/ai-reports/generate/           - сгенерировать для одного студента
POST /api/analytics/ai-reports/generate-for-group/ - сгенерировать для всей группы
GET  /api/analytics/ai-reports/for-student/{id}/   - все отчёты по студенту
```

### Пример запроса

```bash
curl -X POST "https://lectio.space/api/analytics/ai-reports/generate/" \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"student_id": 123, "group_id": 456, "period_days": 30}'
```

## Модель данных

```python
class StudentAIReport:
    student          # FK → User (студент)
    teacher          # FK → User (преподаватель)
    group            # FK → Group (опционально)
    status           # pending|generating|completed|failed
    period_start     # Начало периода анализа
    period_end       # Конец периода анализа
    
    # Статистика
    total_submissions        # Сдано ДЗ
    avg_score_percent        # Средний балл %
    total_questions_answered # Всего ответов
    
    # AI анализ (JSON)
    ai_analysis = {
        "strengths": [...],
        "weaknesses": [...],
        "common_mistakes": [...],
        "recommendations": [...],
        "progress_trend": "improving|stable|declining",
        "summary": "..."
    }
```

## Стоимость

Один отчёт ≈ 1500 токенов → ~$0.002 (DeepSeek)

Группа из 10 студентов ≈ $0.02
