# 👥 План тестирования управления группами и учениками

**Дата создания:** 2 декабря 2025  
**Статус:** Готов к использованию  
**Приоритет:** Высокий

---

## 📋 Содержание

1. [Текущее состояние системы](#текущее-состояние-системы)
2. [Архитектура системы](#архитектура-системы)
3. [Полный тест-план](#полный-тест-план)
4. [Система инвайт-кодов](#система-инвайт-кодов)
5. [API Reference](#api-reference)
6. [Известные проблемы](#известные-проблемы)
7. [Чек-лист деплоя](#чек-лист-деплоя)

---

## 🔍 Текущее состояние системы

### ✅ Что работает

**Frontend Components:**
- ✅ `GroupsManage.js` - полностью переработан в современном стиле
- ✅ `GroupInviteModal.js` - модальное окно с инвайт-кодом
- ✅ `JoinGroupModal.js` - присоединение студента по коду
- ✅ `StudentHomePage.js` - интеграция с группами

**Backend API:**
- ✅ CRUD для групп (`GroupViewSet`)
- ✅ Добавление/удаление студентов (`add_students`, `remove_students`)
- ✅ Генерация инвайт-кодов при создании группы
- ✅ Регенерация кодов (`regenerate_code`)
- ✅ Присоединение по коду (`join_by_code`)

**Модели:**
- ✅ `Group` модель с полем `invite_code`
- ✅ `ManyToMany` связь с `CustomUser` (students)
- ✅ Автоматическая генерация уникального кода

**Функционал:**
- ✅ Учитель может создавать группы
- ✅ Учитель может приглашать учеников через код
- ✅ Ученик может присоединиться по коду
- ✅ Учитель может удалять учеников из группы
- ✅ QR-код генерация для быстрого присоединения

### ❌ Известные проблемы

1. **Ограниченный функционал:**
   - ⚠️ Нет массового импорта учеников (CSV/Excel)
   - ⚠️ Нет экспорта списка группы
   - ⚠️ Нет поиска/фильтров для больших списков
   - ⚠️ Нельзя перенести ученика из одной группы в другую (только удалить + добавить)

2. **UI/UX:**
   - ⚠️ Модальное окно управления учениками использует ID (не user-friendly)
   - ⚠️ Нет подтверждения удаления ученика из группы
   - ⚠️ Отсутствует пагинация для больших списков групп

3. **Валидация:**
   - ⚠️ Можно создать группу с пустым описанием
   - ⚠️ Нет проверки уникальности названия группы
   - ⚠️ При удалении группы не проверяется наличие активных занятий

4. **Безопасность:**
   - ⚠️ Инвайт-код может быть украден и использован кем угодно
   - ⚠️ Нет срока действия кода
   - ⚠️ Нет лимита на количество использований кода

### 🔧 Технические долги

- 📝 Нет unit-тестов для `GroupViewSet`
- 📝 Отсутствует документация API
- 📝 Нет логирования действий с группами
- 📝 Не настроены email-уведомления при добавлении в группу
- 📝 Нет интеграции с Telegram для уведомлений о группах

---

## 🏗️ Архитектура системы

### Схема потока данных

```
┌─────────────────────────────────────────────────────────┐
│                  УЧИТЕЛЬ (Teacher)                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ 1. Создает группу
                  ↓
┌─────────────────────────────────────────────────────────┐
│  POST /api/groups/                                      │
│  {                                                      │
│    "name": "Математика 9А",                            │
│    "description": "Подготовка к ОГЭ"                   │
│  }                                                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ 2. Backend генерирует invite_code
                  ↓
┌─────────────────────────────────────────────────────────┐
│  Group.generate_invite_code()                           │
│  → "X0QPF97P" (8 символов, уникальный)                │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ 3. Учитель открывает GroupInviteModal
                  ↓
┌─────────────────────────────────────────────────────────┐
│  Показывает:                                            │
│  - Код: X0QPF97P                                       │
│  - Ссылка: http://site.com/student?code=X0QPF97P      │
│  - QR-код                                              │
│  - Кнопки копирования                                   │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ 4. Ученик получает код
                  ↓
┌─────────────────────────────────────────────────────────┐
│                  УЧЕНИК (Student)                       │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ 5. Открывает JoinGroupModal
                  ↓
┌─────────────────────────────────────────────────────────┐
│  POST /api/groups/join_by_code/                        │
│  {                                                      │
│    "invite_code": "X0QPF97P"                           │
│  }                                                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  │ 6. Backend проверяет и добавляет
                  ↓
┌─────────────────────────────────────────────────────────┐
│  group.students.add(user)                               │
│  → Студент теперь в группе                            │
└─────────────────────────────────────────────────────────┘
```

### Модель данных

```python
# teaching_panel/schedule/models.py

class Group(models.Model):
    name = models.CharField(max_length=255)
    description = models.TextField(blank=True)
    teacher = models.ForeignKey(
        CustomUser, 
        on_delete=models.CASCADE, 
        related_name='taught_groups'
    )
    students = models.ManyToManyField(
        CustomUser, 
        related_name='student_groups',
        blank=True
    )
    invite_code = models.CharField(
        max_length=8, 
        unique=True, 
        db_index=True
    )
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    def generate_invite_code(self):
        """Генерация уникального 8-значного кода"""
        import random
        import string
        
        while True:
            code = ''.join(random.choices(
                string.ascii_uppercase + string.digits, 
                k=8
            ))
            if not Group.objects.filter(invite_code=code).exists():
                self.invite_code = code
                self.save()
                return code
    
    def save(self, *args, **kwargs):
        if not self.invite_code:
            self.generate_invite_code()
        super().save(*args, **kwargs)
    
    @property
    def student_count(self):
        return self.students.count()
    
    class Meta:
        ordering = ['-created_at']
```

### API Endpoints

| Метод | Endpoint | Описание | Права доступа |
|-------|----------|----------|---------------|
| GET | `/api/groups/` | Список групп | Teacher: свои, Student: где состоит |
| POST | `/api/groups/` | Создать группу | Teacher only |
| GET | `/api/groups/{id}/` | Детали группы | Owner или член группы |
| PUT | `/api/groups/{id}/` | Обновить группу | Owner only |
| DELETE | `/api/groups/{id}/` | Удалить группу | Owner only |
| POST | `/api/groups/{id}/add_students/` | Добавить учеников по ID | Owner only |
| POST | `/api/groups/{id}/remove_students/` | Удалить учеников | Owner only |
| POST | `/api/groups/{id}/regenerate_code/` | Новый инвайт-код | Owner only |
| POST | `/api/groups/join_by_code/` | Присоединиться по коду | Student only |

---

## 📋 Полный тест-план

### Раздел 1: Создание и управление группами (Учитель)

#### Тест 1.1: Создание новой группы

**Предусловия:**
- Пользователь залогинен как учитель
- Перешел на `/groups/manage`

**Шаги:**
1. Убедиться, что открыта вкладка "👥 Группа"
2. Заполнить форму:
   - Название: "Английский A1"
   - Описание: "Подготовка к Elementary уровню"
3. Нажать "Создать группу"

**Ожидаемый результат:**
- ✅ Группа создана и появилась в списке справа
- ✅ Автоматически сгенерирован инвайт-код (8 символов)
- ✅ Счетчик учеников: "0 уч."
- ✅ Доступны кнопки: "✏️ Изменить", "📨 Пригласить", "👥 Ученики", "🗑️ Удалить"

**Проверка в БД:**
```sql
SELECT id, name, description, teacher_id, invite_code 
FROM schedule_group 
ORDER BY created_at DESC 
LIMIT 1;

-- invite_code должен быть уникальным и состоять из 8 символов
```

#### Тест 1.2: Редактирование группы

**Шаги:**
1. Найти созданную группу в списке
2. Нажать "✏️ Изменить"
3. Изменить название на "Английский A1 - Вечерняя группа"
4. Изменить описание на "Занятия по вт и чт с 18:00"
5. Нажать "Сохранить изменения"

**Ожидаемый результат:**
- ✅ Название и описание обновлены
- ✅ Инвайт-код остался прежним (не изменился)
- ✅ Форма очистилась
- ✅ Список групп обновился

#### Тест 1.3: Удаление группы

**Предусловия:**
- Создана тестовая группа без учеников

**Шаги:**
1. Нажать "🗑️ Удалить" на карточке группы
2. Подтвердить удаление в диалоге

**Ожидаемый результат:**
- ✅ Группа удалена из списка
- ✅ Запись удалена из БД

**Проверка:**
```sql
SELECT * FROM schedule_group WHERE name = 'Тестовая группа';
-- Должно быть пусто
```

#### Тест 1.4: Создание группы с пустыми полями

**Шаги:**
1. Оставить поле "Название" пустым
2. Попытаться создать группу

**Ожидаемый результат:**
- ❌ Ошибка валидации: "Название обязательно"
- ✅ Группа не создается

#### Тест 1.5: Создание дубликата группы

**Шаги:**
1. Создать группу "Математика 9А"
2. Попытаться создать еще одну группу с таким же названием

**Ожидаемый результат:**
- ⚠️ **ТЕКУЩЕЕ ПОВЕДЕНИЕ:** Группа создается (нет проверки уникальности)
- 📝 **ЖЕЛАЕМОЕ:** Ошибка "Группа с таким названием уже существует"

---

### Раздел 2: Система инвайт-кодов (Учитель)

#### Тест 2.1: Просмотр инвайт-кода

**Шаги:**
1. Создать или выбрать существующую группу
2. Нажать "📨 Пригласить"

**Ожидаемый результат:**
- ✅ Открывается модальное окно `GroupInviteModal`
- ✅ Отображается:
  - Заголовок: "Пригласить учеников"
  - Подзаголовок: "Группа: [название]"
  - Секция "Код приглашения" с 8-значным кодом
  - Кнопка "📋 Копировать" рядом с кодом
  - Секция "Ссылка-приглашение" с полным URL
  - Кнопка "📋 Копировать" для ссылки
  - Секция "QR-код" с изображением QR
  - Кнопка "🔄 Создать новый код"
  - Кнопка "Готово"

#### Тест 2.2: Копирование инвайт-кода

**Шаги:**
1. Открыть модальное окно приглашения
2. Нажать "📋 Копировать" рядом с кодом

**Ожидаемый результат:**
- ✅ Код скопирован в буфер обмена
- ✅ Кнопка меняется на "✓ Скопировано" на 2 секунды
- ✅ Можно вставить код в любое поле (Ctrl+V)

**Проверка:**
```javascript
// В браузерной консоли
navigator.clipboard.readText().then(text => {
  console.log('Clipboard:', text); // Должен быть код вида "X0QPF97P"
});
```

#### Тест 2.3: Копирование ссылки-приглашения

**Шаги:**
1. Нажать "📋 Копировать" рядом со ссылкой

**Ожидаемый результат:**
- ✅ Ссылка скопирована: `http://72.56.81.163/student?code=X0QPF97P`
- ✅ При вставке в браузер открывается ЛК студента с автозаполненным кодом

#### Тест 2.4: QR-код генерация

**Шаги:**
1. Проверить наличие QR-кода в модальном окне
2. Навести курсор на QR-код

**Ожидаемый результат:**
- ✅ QR-код отображается как изображение
- ✅ Изображение загружено через API: `https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=...`
- ✅ При сканировании QR-кода телефоном открывается ссылка с кодом

**Тест со смартфоном:**
1. Открыть камеру на телефоне
2. Навести на QR-код на экране компьютера
3. Тапнуть по уведомлению

**Ожидаемый результат:**
- ✅ Открывается браузер с URL: `http://72.56.81.163/student?code=X0QPF97P`

#### Тест 2.5: Регенерация инвайт-кода

**Шаги:**
1. Запомнить текущий код (например: "ABC123XY")
2. Нажать "🔄 Создать новый код"
3. Подтвердить действие в диалоге

**Ожидаемый результат:**
- ✅ Код изменился (например: "Z9M8N7K6")
- ✅ Новый код отличается от старого
- ✅ Старый код больше не работает (проверить в Тесте 3.1)

**Проверка в БД:**
```sql
SELECT invite_code FROM schedule_group WHERE id = <group_id>;
-- Должен быть новый код
```

**Проверка старого кода:**
```bash
curl -X POST http://127.0.0.1:8000/api/groups/join_by_code/ \
  -H "Authorization: Bearer <student_token>" \
  -H "Content-Type: application/json" \
  -d '{"invite_code": "ABC123XY"}'

# Ожидаемый результат: 404 Not Found
```

---

### Раздел 3: Присоединение к группе (Ученик)

#### Тест 3.1: Присоединение по коду (успешный сценарий)

**Предусловия:**
- Пользователь залогинен как ученик
- Учитель создал группу и предоставил инвайт-код "X0QPF97P"

**Шаги:**
1. Перейти на `/student` (ЛК ученика)
2. Найти секцию "Список курсов"
3. Нажать кнопку "Есть промокод?"
4. В модальном окне ввести код: "X0QPF97P"
5. Нажать "Присоединиться"

**Ожидаемый результат:**
- ✅ Появляется сообщение: "Вы успешно присоединились к группе!"
- ✅ Модальное окно закрывается через 1.5 секунды
- ✅ Группа появляется в списке курсов
- ✅ Отображается название группы и имя учителя

**Проверка в БД:**
```sql
SELECT * FROM schedule_group_students 
WHERE group_id = <group_id> AND customuser_id = <student_id>;

-- Должна быть запись
```

#### Тест 3.2: Присоединение с неправильным кодом

**Шаги:**
1. Открыть `JoinGroupModal`
2. Ввести несуществующий код: "WRONGCOD"
3. Нажать "Присоединиться"

**Ожидаемый результат:**
- ❌ Ошибка: "Группа с таким кодом не найдена"
- ✅ Модальное окно остается открытым
- ✅ Ученик НЕ добавлен ни в какую группу

#### Тест 3.3: Присоединение с пустым кодом

**Шаги:**
1. Оставить поле кода пустым
2. Нажать "Присоединиться"

**Ожидаемый результат:**
- ❌ Ошибка: "Введите код приглашения"
- ✅ Запрос на сервер не отправляется

#### Тест 3.4: Повторное присоединение к той же группе

**Предусловия:**
- Ученик уже состоит в группе "Английский A1"

**Шаги:**
1. Попытаться присоединиться к той же группе еще раз с тем же кодом

**Ожидаемый результат:**
- ⚠️ Сообщение: "Вы уже состоите в этой группе"
- ✅ Статус 200 OK (не ошибка)
- ✅ Дубликат НЕ создается в БД

**Проверка:**
```sql
SELECT COUNT(*) FROM schedule_group_students 
WHERE group_id = <group_id> AND customuser_id = <student_id>;

-- Должно быть ровно 1
```

#### Тест 3.5: Присоединение через прямую ссылку

**Шаги:**
1. Учитель копирует ссылку: `http://72.56.81.163/student?code=X0QPF97P`
2. Отправляет ученику через WhatsApp/Telegram
3. Ученик кликает по ссылке (еще не залогинен)

**Ожидаемый результат:**
- ✅ Открывается `/student`
- ✅ Если не залогинен → редирект на `/auth`
- ✅ После логина → возврат на `/student`
- ⚠️ **ТЕКУЩАЯ ПРОБЛЕМА:** Код из URL параметра `?code=` НЕ подставляется автоматически
- 📝 **ЖЕЛАЕМОЕ:** Автоматически открыть модальное окно с предзаполненным кодом

**Исправление (TODO):**
```javascript
// В StudentHomePage.js
useEffect(() => {
  const params = new URLSearchParams(window.location.search);
  const code = params.get('code');
  if (code) {
    setShowJoinModal(true);
    // Передать код в JoinGroupModal как prop
  }
}, []);
```

#### Тест 3.6: Присоединение учителя к группе (негативный)

**Предусловия:**
- Пользователь залогинен как учитель

**Шаги:**
1. Попытаться использовать `POST /api/groups/join_by_code/`

**Ожидаемый результат:**
- ❌ Ошибка 403 Forbidden
- ❌ Сообщение: "Только ученики могут присоединяться к группам"

---

### Раздел 4: Управление учениками в группе (Учитель)

#### Тест 4.1: Просмотр списка учеников

**Предусловия:**
- Группа "Английский A1" содержит 3 учеников

**Шаги:**
1. Найти группу в списке
2. Нажать "👥 Ученики"

**Ожидаемый результат:**
- ✅ Открывается модальное окно "Управление учениками"
- ✅ Отображается название группы в заголовке
- ✅ Есть информация: "💡 Как добавить учеников: Нажмите 'Пригласить'..."
- ✅ Секция "Удалить учеников (ID через запятую)"
- ✅ Кнопка "❌ Удалить из группы"

**Примечание:**
⚠️ Текущая версия использует удаление по ID, что не user-friendly.
📝 **Планируется:** Показывать список учеников с чекбоксами для удаления.

#### Тест 4.2: Удаление ученика из группы по ID

**Предусловия:**
- Ученик с ID=5 состоит в группе

**Шаги:**
1. Открыть модальное окно "Управление учениками"
2. В поле "Удалить учеников" ввести: "5"
3. Нажать "❌ Удалить из группы"

**Ожидаемый результат:**
- ✅ Ученик удален из группы
- ✅ Счетчик учеников уменьшился (было "3 уч.", стало "2 уч.")
- ✅ Модальное окно обновилось

**Проверка в БД:**
```sql
SELECT * FROM schedule_group_students 
WHERE group_id = <group_id> AND customuser_id = 5;

-- Должно быть пусто
```

#### Тест 4.3: Удаление нескольких учеников

**Шаги:**
1. Ввести несколько ID через запятую: "5, 7, 9"
2. Нажать "Удалить"

**Ожидаемый результат:**
- ✅ Все 3 ученика удалены
- ✅ Счетчик обновился корректно

#### Тест 4.4: Удаление несуществующего ID

**Шаги:**
1. Ввести ID=999 (не существует)
2. Нажать "Удалить"

**Ожидаемый результат:**
- ✅ Ошибки не возникает
- ✅ Backend игнорирует несуществующий ID (try/except в коде)
- ✅ Остальные ученики остаются в группе

#### Тест 4.5: Попытка добавить ученика по ID (устаревший метод)

**Примечание:**
⚠️ В текущей версии кнопка "Добавить учеников" скрыта, так как используется только система инвайт-кодов.

**Если раскомментировать секцию:**
```javascript
// В GroupsManage.js закомментирована секция:
/*
<div className="gm-modal-column" style={{width: '100%'}}>
  <label className="gm-modal-label">➕ Добавить учеников (ID через запятую)</label>
  <input
    className="gm-modal-input"
    value={addIds}
    onChange={(e) => setAddIds(e.target.value)}
    placeholder="1, 2, 3"
  />
  <button ... onClick={commitAddStudents}>
    ➕ Добавить в группу
  </button>
</div>
*/
```

**Тест (если функция активна):**
1. Ввести ID=10
2. Нажать "Добавить"

**Ожидаемый результат:**
- ✅ Ученик добавлен
- ✅ Счетчик обновился

---

### Раздел 5: Создание учеников (Учитель)

#### Тест 5.1: Создание нового ученика

**Шаги:**
1. Переключиться на вкладку "🎓 Ученик"
2. Заполнить форму:
   - Email: `newstudent@test.com`
   - Имя: `Иван`
   - Фамилия: `Петров`
   - Пароль: `securepass123`
3. Нажать "👤 Создать ученика"

**Ожидаемый результат:**
- ✅ Ученик создан в системе
- ✅ Роль автоматически установлена `student`
- ✅ Можно залогиниться с указанным email и паролем
- ✅ Форма очистилась

**Проверка в БД:**
```sql
SELECT id, email, first_name, last_name, role 
FROM accounts_customuser 
WHERE email = 'newstudent@test.com';

-- role должен быть 'student'
```

#### Тест 5.2: Создание ученика с существующим email

**Шаги:**
1. Попытаться создать ученика с email, который уже существует

**Ожидаемый результат:**
- ❌ Ошибка: "Пользователь с таким email уже существует"
- ✅ Ученик не создается

#### Тест 5.3: Создание ученика с невалидным email

**Шаги:**
1. Ввести email: `notanemail`
2. Попытаться создать

**Ожидаемый результат:**
- ❌ Ошибка валидации: "Введите корректный email"

#### Тест 5.4: Создание ученика без имени

**Шаги:**
1. Оставить поле "Имя" пустым
2. Заполнить остальные поля
3. Создать

**Ожидаемый результат:**
- ⚠️ **ТЕКУЩЕЕ ПОВЕДЕНИЕ:** Ученик создается с пустым именем
- 📝 **ЖЕЛАЕМОЕ:** Ошибка "Имя обязательно"

---

### Раздел 6: Edge Cases (Граничные случаи)

#### Тест 6.1: Удаление группы с учениками

**Предусловия:**
- Группа "Математика 10А" содержит 15 учеников

**Шаги:**
1. Нажать "🗑️ Удалить" на карточке группы
2. Подтвердить удаление

**Ожидаемый результат:**
- ✅ Группа удалена
- ✅ Связи в таблице `schedule_group_students` удалены (каскадное удаление)
- ✅ Ученики остаются в системе (не удаляются)

**Проверка:**
```sql
-- Группа удалена
SELECT * FROM schedule_group WHERE id = <group_id>;
-- Пусто

-- Связи удалены
SELECT * FROM schedule_group_students WHERE group_id = <group_id>;
-- Пусто

-- Ученики остались
SELECT COUNT(*) FROM accounts_customuser WHERE id IN (5, 7, 9, ...);
-- Все на месте
```

#### Тест 6.2: Удаление группы с активными занятиями

**Предусловия:**
- Группа имеет запланированные занятия (уроки)

**Шаги:**
1. Попытаться удалить группу

**Ожидаемый результат:**
- ⚠️ **ТЕКУЩЕЕ ПОВЕДЕНИЕ:** Группа удаляется, занятия остаются "сиротами"
- 📝 **ЖЕЛАЕМОЕ:** Предупреждение "В группе есть активные занятия. Удалить все равно?"

**Рекомендация:**
Добавить проверку перед удалением:
```python
# В GroupViewSet.destroy()
def destroy(self, request, *args, **kwargs):
    group = self.get_object()
    lesson_count = group.lessons.count()
    
    if lesson_count > 0:
        return Response({
            'error': f'Невозможно удалить группу. В ней {lesson_count} активных занятий.'
        }, status=400)
    
    return super().destroy(request, *args, **kwargs)
```

#### Тест 6.3: Большое количество групп (100+)

**Предусловия:**
- Создано 150 групп

**Шаги:**
1. Открыть `/groups/manage`
2. Прокрутить список групп

**Ожидаемый результат:**
- ⚠️ **ТЕКУЩАЯ ПРОБЛЕМА:** Все 150 групп загружаются сразу → медленно
- ✅ Прокрутка работает (но лагает)
- 📝 **ЖЕЛАЕМОЕ:** Пагинация (показывать по 20 групп)

#### Тест 6.4: Большое количество учеников в группе (500+)

**Предусловия:**
- Группа содержит 500 учеников

**Шаги:**
1. Открыть модальное окно "Управление учениками"

**Ожидаемый результат:**
- ⚠️ Модальное окно может грузиться медленно
- 📝 **РЕКОМЕНДАЦИЯ:** Не показывать список всех учеников, только счетчик

#### Тест 6.5: Одновременное присоединение к группе

**Сценарий:**
- 50 учеников одновременно вводят один и тот же инвайт-код

**Ожидаемый результат:**
- ✅ Все 50 учеников успешно добавлены
- ✅ Нет дубликатов в таблице `schedule_group_students`
- ✅ Backend обрабатывает конкурентные запросы корректно (благодаря ManyToMany)

#### Тест 6.6: Регенерация кода во время присоединения

**Сценарий:**
1. Ученик вводит код "ABC123XY"
2. В этот момент учитель регенерирует код
3. Ученик нажимает "Присоединиться"

**Ожидаемый результат:**
- ❌ Ошибка: "Группа с таким кодом не найдена"
- ✅ Ученик НЕ добавлен в группу
- 📝 **РЕКОМЕНДАЦИЯ:** Показать учителю предупреждение перед регенерацией

---

### Раздел 7: Производительность

#### Тест 7.1: Время загрузки списка групп

**Метрика:** <1 секунда для 50 групп

**Шаги:**
1. Открыть DevTools → Network
2. Перейти на `/groups/manage`
3. Замерить время запроса `GET /api/groups/`

**Ожидаемый результат:**
- ✅ <1 секунда

#### Тест 7.2: Время присоединения к группе

**Метрика:** <500ms

**Шаги:**
1. Ввести код в `JoinGroupModal`
2. Замерить время запроса `POST /api/groups/join_by_code/`

**Ожидаемый результат:**
- ✅ <500ms

#### Тест 7.3: Время удаления 10 учеников

**Шаги:**
1. Ввести 10 ID через запятую
2. Нажать "Удалить"
3. Замерить время

**Ожидаемый результат:**
- ✅ <2 секунды

---

## 🔧 Система инвайт-кодов - Детальная документация

### Алгоритм генерации кода

```python
def generate_invite_code(self):
    """
    Генерация уникального инвайт-кода
    
    Формат: 8 символов (A-Z, 0-9)
    Пример: X0QPF97P
    
    Особенности:
    - Только заглавные буквы и цифры (для удобства ввода)
    - Длина 8 символов (баланс между удобством и уникальностью)
    - Проверка уникальности в цикле
    - Автоматическая генерация при создании группы
    """
    import random
    import string
    
    max_attempts = 100
    attempts = 0
    
    while attempts < max_attempts:
        code = ''.join(random.choices(
            string.ascii_uppercase + string.digits, 
            k=8
        ))
        
        # Проверка уникальности
        if not Group.objects.filter(invite_code=code).exists():
            self.invite_code = code
            self.save()
            return code
        
        attempts += 1
    
    # Если не удалось за 100 попыток (очень маловероятно)
    raise Exception("Не удалось сгенерировать уникальный код")
```

### Вероятность коллизий

**Расчет:**
- Алфавит: 26 букв + 10 цифр = 36 символов
- Длина кода: 8 символов
- Возможных комбинаций: 36^8 = 2,821,109,907,456 (2.8 триллионов)

**При 1 миллионе групп:**
- Вероятность коллизии: 1 / 2,821,109,907 ≈ 0.00000035%
- Практически невозможно

### Безопасность

**Потенциальные угрозы:**

1. **Brute-force атака:**
   - Атакующий пытается угадать код перебором
   - Защита: Rate limiting (макс 5 попыток в минуту на IP)

2. **Утечка кода:**
   - Код может быть случайно опубликован в открытом доступе
   - Защита: Возможность регенерации кода

3. **Переиспользование кода:**
   - Старые коды могут быть использованы повторно
   - Текущее поведение: Код живет вечно
   - Рекомендация: Добавить поле `code_expires_at`

**Рекомендуемые улучшения:**

```python
# Добавить в модель Group
class Group(models.Model):
    # ... существующие поля
    
    code_expires_at = models.DateTimeField(null=True, blank=True)
    max_code_uses = models.IntegerField(default=0)  # 0 = unlimited
    code_use_count = models.IntegerField(default=0)
    
    def is_code_valid(self):
        """Проверка валидности кода"""
        # Проверка срока действия
        if self.code_expires_at and timezone.now() > self.code_expires_at:
            return False
        
        # Проверка лимита использований
        if self.max_code_uses > 0 and self.code_use_count >= self.max_code_uses:
            return False
        
        return True
```

---

## 📡 API Reference

### POST /api/groups/

**Создание новой группы**

**Request:**
```json
{
  "name": "Математика 9А",
  "description": "Подготовка к ОГЭ"
}
```

**Response 201:**
```json
{
  "id": 42,
  "name": "Математика 9А",
  "description": "Подготовка к ОГЭ",
  "teacher": {
    "id": 1,
    "email": "teacher@example.com",
    "first_name": "Иван",
    "last_name": "Иванов"
  },
  "students": [],
  "invite_code": "X0QPF97P",
  "student_count": 0,
  "created_at": "2025-12-02T18:30:00Z"
}
```

### GET /api/groups/

**Список групп**

**Query Parameters:**
- `search` - поиск по названию
- `page` - номер страницы (если включена пагинация)

**Response 200:**
```json
{
  "count": 15,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 42,
      "name": "Математика 9А",
      "description": "Подготовка к ОГЭ",
      "teacher": { /* ... */ },
      "students": [
        {
          "id": 5,
          "email": "student1@example.com",
          "first_name": "Петр",
          "last_name": "Петров"
        }
      ],
      "invite_code": "X0QPF97P",
      "student_count": 1,
      "created_at": "2025-12-02T18:30:00Z"
    }
  ]
}
```

### PUT /api/groups/{id}/

**Обновление группы**

**Request:**
```json
{
  "name": "Математика 9А - Вечерняя",
  "description": "Занятия вт-чт 18:00"
}
```

**Response 200:**
```json
{
  "id": 42,
  "name": "Математика 9А - Вечерняя",
  "description": "Занятия вт-чт 18:00",
  /* ... остальные поля */
}
```

### DELETE /api/groups/{id}/

**Удаление группы**

**Response 204:** No Content

### POST /api/groups/{id}/add_students/

**Добавление учеников в группу (по ID)**

**Request:**
```json
{
  "student_ids": [5, 7, 9]
}
```

**Response 200:**
```json
{
  "id": 42,
  "name": "Математика 9А",
  "students": [
    { "id": 5, "email": "student1@example.com", /* ... */ },
    { "id": 7, "email": "student2@example.com", /* ... */ },
    { "id": 9, "email": "student3@example.com", /* ... */ }
  ],
  "student_count": 3,
  /* ... */
}
```

### POST /api/groups/{id}/remove_students/

**Удаление учеников из группы**

**Request:**
```json
{
  "student_ids": [5, 7]
}
```

**Response 200:**
```json
{
  "id": 42,
  "name": "Математика 9А",
  "students": [
    { "id": 9, "email": "student3@example.com", /* ... */ }
  ],
  "student_count": 1,
  /* ... */
}
```

### POST /api/groups/{id}/regenerate_code/

**Регенерация инвайт-кода**

**Request:** (пустой body)

**Response 200:**
```json
{
  "invite_code": "Z9M8N7K6",
  "message": "Новый код приглашения создан"
}
```

### POST /api/groups/join_by_code/

**Присоединение к группе по коду**

**Request:**
```json
{
  "invite_code": "X0QPF97P"
}
```

**Response 200 (успешное присоединение):**
```json
{
  "message": "Вы успешно присоединились к группе \"Математика 9А\"",
  "group": {
    "id": 42,
    "name": "Математика 9А",
    "description": "Подготовка к ОГЭ",
    "teacher": { /* ... */ },
    /* ... */
  }
}
```

**Response 200 (уже состоит):**
```json
{
  "message": "Вы уже состоите в этой группе"
}
```

**Response 404 (код не найден):**
```json
{
  "error": "Группа с таким кодом не найдена"
}
```

**Response 403 (не студент):**
```json
{
  "error": "Только ученики могут присоединяться к группам"
}
```

---

## ❌ Известные проблемы и улучшения

### Критические

1. **❌ Нет пагинации для больших списков**
   - При 100+ группах страница грузится медленно
   - Решение: Добавить DRF пагинацию

2. **❌ Управление учениками через ID не user-friendly**
   - Учитель должен знать ID ученика
   - Решение: Показать список с чекбоксами

3. **❌ Автозаполнение кода из URL не работает**
   - При клике на ссылку `?code=X0QPF97P` код не подставляется
   - Решение: Парсить URL параметры в `StudentHomePage`

### Средний приоритет

4. **⚠️ Нет подтверждения при удалении ученика**
   - Можно случайно удалить не того ученика
   - Решение: Добавить `window.confirm()`

5. **⚠️ Нет проверки уникальности названия группы**
   - Можно создать 10 групп "Математика 9А"
   - Решение: Добавить валидацию на backend

6. **⚠️ При удалении группы не проверяются активные занятия**
   - Занятия остаются "сиротами"
   - Решение: Добавить проверку в `destroy()`

### Низкий приоритет

7. **📝 Нет поиска по группам**
   - В списке из 50 групп сложно найти нужную
   - Решение: Добавить поле поиска

8. **📝 Нет фильтров**
   - Не показать только группы с учениками
   - Решение: Dropdown фильтры

9. **📝 Нет экспорта списка группы**
   - Нельзя выгрузить список учеников в Excel
   - Решение: Кнопка "Экспорт" → генерация XLSX

10. **📝 Нет массового импорта учеников**
    - Нужно вручную создавать каждого ученика
    - Решение: Загрузка CSV/Excel

---

## 🎯 Рекомендуемые улучшения

### Улучшение 1: Список учеников с чекбоксами

**Текущее поведение:**
```
┌─────────────────────────────────────┐
│ Удалить учеников (ID через запятую) │
│ [_____________________________]     │
│ [❌ Удалить из группы]              │
└─────────────────────────────────────┘
```

**Предлагаемое:**
```
┌──────────────────────────────────────────┐
│ Ученики в группе (3):                    │
│                                          │
│ [ ] Иван Петров (ivan@test.com)        │
│ [ ] Мария Сидорова (maria@test.com)    │
│ [ ] Алексей Смирнов (alex@test.com)    │
│                                          │
│ [❌ Удалить выбранных]                  │
└──────────────────────────────────────────┘
```

**Код:**
```javascript
const [selectedStudents, setSelectedStudents] = useState([]);

const toggleStudent = (studentId) => {
  setSelectedStudents(prev => 
    prev.includes(studentId)
      ? prev.filter(id => id !== studentId)
      : [...prev, studentId]
  );
};

// В JSX
{studentOpsGroup.students.map(student => (
  <label key={student.id} className="student-checkbox">
    <input
      type="checkbox"
      checked={selectedStudents.includes(student.id)}
      onChange={() => toggleStudent(student.id)}
    />
    <span>{student.first_name} {student.last_name}</span>
    <span className="student-email">({student.email})</span>
  </label>
))}
```

### Улучшение 2: Автозаполнение кода из URL

**Файл:** `frontend/src/components/StudentHomePage.js`

```javascript
import { useSearchParams } from 'react-router-dom';

const StudentHomePage = () => {
  const [searchParams, setSearchParams] = useSearchParams();
  const [showJoinModal, setShowJoinModal] = useState(false);
  const [prefilledCode, setPrefilledCode] = useState('');
  
  useEffect(() => {
    const code = searchParams.get('code');
    if (code) {
      setPrefilledCode(code.trim().toUpperCase());
      setShowJoinModal(true);
      // Очистить URL параметр после использования
      setSearchParams({});
    }
  }, [searchParams, setSearchParams]);
  
  // ...
  
  return (
    <>
      {/* ... */}
      {showJoinModal && (
        <JoinGroupModal
          onClose={() => setShowJoinModal(false)}
          onSuccess={handleJoinSuccess}
          initialCode={prefilledCode}
        />
      )}
    </>
  );
};
```

**Обновить JoinGroupModal.js:**
```javascript
const JoinGroupModal = ({ onClose, onSuccess, initialCode = '' }) => {
  const [inviteCode, setInviteCode] = useState(initialCode);
  // ... остальной код
};
```

### Улучшение 3: Пагинация

**Backend:**
```python
# В settings.py
REST_FRAMEWORK = {
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20
}
```

**Frontend:**
```javascript
const [page, setPage] = useState(1);

const loadGroups = async () => {
  const res = await getGroups({ page });
  setGroups(res.data.results);
  setTotalPages(Math.ceil(res.data.count / 20));
};

// В JSX
<div className="pagination">
  <button disabled={page === 1} onClick={() => setPage(page - 1)}>
    ← Назад
  </button>
  <span>Страница {page} из {totalPages}</span>
  <button disabled={page === totalPages} onClick={() => setPage(page + 1)}>
    Вперед →
  </button>
</div>
```

---

## ✅ Чек-лист деплоя

### Перед деплоем

- [ ] Все тесты из Раздела 1-7 прошли локально
- [ ] Проверена работа на staging
- [ ] Миграции БД подготовлены
- [ ] Frontend собран без ошибок

### Backend

- [ ] Применены миграции: `python manage.py migrate`
- [ ] Проверен индекс на `invite_code`:
  ```sql
  SELECT indexname FROM pg_indexes WHERE tablename = 'schedule_group';
  ```
- [ ] Логирование настроено
- [ ] Rate limiting настроен для `/join_by_code/`

### Frontend

- [ ] `GroupsManage.js` обновлен
- [ ] `GroupInviteModal.js` и `JoinGroupModal.js` работают
- [ ] QR-код генерируется корректно
- [ ] Prod build собран: `npm run build`

### Тестирование на production

- [ ] Создать группу и проверить генерацию кода
- [ ] Присоединиться к группе с телефона через QR
- [ ] Проверить удаление ученика
- [ ] Регенерировать код и убедиться, что старый не работает

---

## 📊 Метрики успеха

| Метрика | Целевое значение | Как измерить |
|---------|------------------|--------------|
| Время создания группы | <1 сек | Network tab |
| Время присоединения | <500ms | Network tab |
| Успешность присоединения | >98% | Analytics |
| Время загрузки списка групп | <1 сек для 50 групп | Lighthouse |
| Процент регенераций кодов | <5% | DB query |

---

## 🔄 План обновлений

### Краткосрочные (1-2 недели)

1. Исправить автозаполнение кода из URL
2. Добавить подтверждение удаления ученика
3. Показать список учеников с чекбоксами

### Среднесрочные (1 месяц)

1. Пагинация для больших списков
2. Поиск и фильтры
3. Экспорт списка группы в Excel

### Долгосрочные (2-3 месяца)

1. Массовый импорт учеников из CSV
2. Срок действия инвайт-кодов
3. Лимит использований кода
4. Email/Telegram уведомления при добавлении в группу

---

**Документ создан:** 2 декабря 2025  
**Последнее обновление:** 2 декабря 2025  
**Автор:** GitHub Copilot + команда Teaching Panel  
**Статус:** ✅ Готов к использованию
