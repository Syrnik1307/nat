# 🚀 Lectio Space Scaling Configurations

Этот каталог содержит готовые конфигурации для разных этапов масштабирования.

## Stages

| Stage | Users | Concurrent | RPS | Server Spec |
|-------|-------|------------|-----|-------------|
| **A (MVP)** | 3,300 | 495 | 50-80 | 2 vCPU / 4 GB |
| **B (Growth)** | 8,250 | 1,237 | 120-180 | 4 vCPU / 8 GB |
| **C (Scale)** | 16,500 | 2,475 | 250-400 | 8 vCPU / 16 GB |

## Quick Deploy

```bash
# Stage A (текущий)
./deploy-stage-a.sh

# Stage B (рост)
./deploy-stage-b.sh

# Stage C (масштаб)
./deploy-stage-c.sh
```

## Файлы

- `stage-a/` - MVP конфигурация (300 учителей / 3000 учеников)
- `stage-b/` - Growth конфигурация + PgBouncer
- `stage-c/` - Scale конфигурация + разделение воркеров
- `pgbouncer/` - PgBouncer конфигурация (нужен на Stage B+)
- `postgresql/` - PostgreSQL tuning для каждого stage

## Миграция между stages

1. **A → B**: Добавить PgBouncer, увеличить workers
2. **B → C**: Разделить Celery по серверам, добавить read replica
