# 💬 Модуль Chat System - Техническое задание

## 👤 Исполнитель: Напарник #2

---

## 🎯 Общая концепция

Система чата между преподавателями и учениками с:
- Личные сообщения (1-на-1)
- Групповые чаты (преподаватель + вся группа)
- Отправка файлов (документы, изображения)
- Real-time обновления через WebSocket
- Уведомления о новых сообщениях
- История переписки

---

## 🏗️ Архитектура

### Backend Stack:
- **Django Channels** - WebSocket поддержка
- **Redis** - pub/sub для сообщений
- **PostgreSQL** - хранение истории
- **Celery** - отправка уведомлений

### Frontend Stack:
- **WebSocket** - real-time коммуникация
- **React Context** - состояние чатов
- **IndexedDB** - локальное кэширование

---

## 📋 Функциональные требования

### 1️⃣ Список чатов (ChatList)

**Для преподавателя:**
- Видит всех студентов из своих групп
- Может начать чат с любым студентом
- Видит групповые чаты своих групп

**Для студента:**
- Видит своих преподавателей
- Видит групповые чаты своих групп
- Не может писать другим студентам (опционально)

**UI элементы:**
- Аватар собеседника
- Имя и роль
- Последнее сообщение (превью)
- Время последнего сообщения
- Счетчик непрочитанных (badge)
- Индикатор онлайн/оффлайн
- Поиск по чатам

**Промпт для разработки:**
```
Создай компонент ChatList.js для списка чатов.

Структура:
1. Header:
   - Заголовок "Сообщения"
   - Поиск по чатам
   - Фильтры: Все / Непрочитанные / Группы

2. Список чатов:
   - Аватар (цветной круг с инициалами)
   - Имя собеседника
   - Роль (teacher/student badge)
   - Превью последнего сообщения (обрезать до 50 символов)
   - Время (относительное: "5 мин назад", "вчера", "21.10.25")
   - Счетчик непрочитанных (оранжевый badge)
   - Индикатор онлайн (зеленая точка)

3. Интерактив:
   - Клик открывает ChatWindow
   - Hover эффект
   - Активный чат выделен

API:
- GET /api/chat/conversations/
Response: {
    "results": [
        {
            "id": 1,
            "participant": {
                "id": 5,
                "name": "Иван Иванов",
                "role": "student",
                "avatar_color": "#3b82f6",
                "is_online": true
            },
            "last_message": {
                "text": "Спасибо за объяснение!",
                "timestamp": "2025-11-14T10:30:00Z",
                "is_read": true
            },
            "unread_count": 0,
            "is_group": false
        }
    ]
}

Используй:
- WebSocket для real-time обновлений
- Виртуализация списка для большого количества чатов (react-window)
- Локальное кэширование в IndexedDB
```

---

### 2️⃣ Окно чата (ChatWindow)

**UI элементы:**
- Header с именем собеседника
- История сообщений (скролл)
- Поле ввода сообщения
- Кнопка отправки файла
- Индикатор "печатает..."
- Статусы сообщений (отправлено ✓, прочитано ✓✓)

**Типы сообщений:**
- Текст
- Изображение (превью, клик для увеличения)
- Документ (иконка + название + размер)
- Системное (например: "Преподаватель начал урок")

**Промпт для разработки:**
```
Создай компонент ChatWindow.js для окна переписки.

Структура:
1. Header:
   - Аватар собеседника
   - Имя и статус (онлайн/был(а) 5 мин назад)
   - Кнопка "..." (меню: Очистить историю, Блокировать - опционально)

2. Область сообщений:
   - Автоскролл вниз при новых сообщениях
   - Группировка по дате ("Сегодня", "Вчера", "21.10.2025")
   - Свои сообщения справа (оранжевый фон)
   - Чужие сообщения слева (серый фон)
   - Время под каждым сообщением
   - Статусы: ✓ отправлено, ✓✓ прочитано

3. Типы сообщений:
   - Текст: обычный текст, markdown поддержка
   - Изображение: превью, клик открывает в полном размере
   - Документ: иконка файла, название, размер, кнопка скачать
   - Системное: центрированное, серый текст

4. Индикатор "печатает...":
   - Показывать когда собеседник печатает
   - Анимированные точки

5. Footer (поле ввода):
   - Textarea с auto-resize
   - Плейсхолдер "Напишите сообщение..."
   - Кнопка отправки (Enter или клик)
   - Кнопка прикрепить файл
   - Счетчик символов (опционально)

WebSocket события:
- message.new - новое сообщение
- message.read - сообщение прочитано
- typing.start - начал печатать
- typing.stop - перестал печатать

API:
- GET /api/chat/messages/?conversation_id=X&page=1
- POST /api/chat/messages/
- PATCH /api/chat/messages/{id}/read/
- WebSocket: ws://localhost:8000/ws/chat/{conversation_id}/

Используй:
- react-markdown для форматирования текста
- Infinite scroll для истории (react-infinite-scroll-component)
- Debounce для индикатора "печатает"
```

---

### 3️⃣ Поле ввода сообщения (MessageInput)

**Фичи:**
- Автоматический рост при многострочном тексте
- Отправка по Enter (Shift+Enter = новая строка)
- Поддержка упоминаний @username
- Эмодзи пикер
- Предпросмотр файлов перед отправкой

**Промпт для разработки:**
```
Создай компонент MessageInput.js для ввода и отправки сообщений.

Структура:
1. Textarea с auto-resize:
   - Минимум 1 строка, максимум 5 строк
   - При превышении появляется скролл
   - Placeholder "Напишите сообщение..."

2. Панель инструментов:
   - Кнопка эмодзи (открывает emoji-picker)
   - Кнопка прикрепить файл
   - Кнопка отправки (активна только если есть текст/файл)

3. Логика отправки:
   - Enter - отправить
   - Shift+Enter - новая строка
   - Очистить поле после отправки
   - Фокус остается в поле

4. Упоминания @:
   - При вводе @ показать список участников
   - Автодополнение имени
   - Выделение упоминаний синим цветом

5. Предпросмотр файлов:
   - Показать превью выбранного файла
   - Кнопка удалить файл (крестик)
   - Валидация: макс 10MB, разрешенные типы

6. Индикатор "печатает":
   - Отправлять WebSocket событие при начале печати
   - Отправлять "перестал" если 3 секунды нет ввода

Используй:
- emoji-picker-react для эмодзи
- react-textarea-autosize для auto-resize
- Lodash debounce для "печатает"

Валидация файлов:
- Изображения: jpg, png, gif, webp (макс 5MB)
- Документы: pdf, doc, docx, xls, xlsx, txt (макс 10MB)
```

---

### 4️⃣ Загрузка файлов (FileUpload)

**Фичи:**
- Drag & Drop
- Множественная загрузка
- Прогресс-бар
- Превью для изображений
- Сжатие изображений на клиенте

**Промпт для разработки:**
```
Создай компонент FileUpload.js для загрузки файлов в чат.

Структура:
1. Drag & Drop зона:
   - Пунктирная рамка
   - Иконка и текст "Перетащите файлы сюда"
   - Подсветка при наведении файла

2. Или кнопка "Выбрать файлы":
   - Открывает file picker
   - Можно выбрать несколько файлов

3. Превью выбранных файлов:
   - Миниатюра для изображений
   - Иконка для документов
   - Название файла
   - Размер файла
   - Кнопка удалить (крестик)
   - Прогресс-бар загрузки

4. Валидация:
   - Проверка типа файла
   - Проверка размера
   - Показать ошибку если не валиден

5. Сжатие изображений:
   - Если изображение > 1MB, сжать до 1MB
   - Сохранить пропорции
   - Качество 85%

6. Загрузка:
   - POST /api/chat/upload/
   - FormData с файлами
   - Показать прогресс (onUploadProgress)
   - После загрузки отправить сообщение с файлом

Используй:
- react-dropzone для drag & drop
- browser-image-compression для сжатия
- axios для загрузки с прогрессом

Формат ответа API:
{
    "file_id": "abc123",
    "file_url": "https://cdn.example.com/files/abc123.jpg",
    "file_name": "screenshot.jpg",
    "file_size": 245789,
    "mime_type": "image/jpeg"
}
```

---

### 5️⃣ Уведомления (ChatNotifications)

**Типы уведомлений:**
- Новое сообщение (если чат не открыт)
- Упоминание @username
- Desktop notifications (если разрешено)
- Звуковой сигнал

**Промпт для разработки:**
```
Создай компонент ChatNotifications.js для уведомлений о новых сообщениях.

Структура:
1. Toast уведомления (внутри приложения):
   - Аватар отправителя
   - Имя отправителя
   - Текст сообщения (обрезать до 100 символов)
   - Кнопка "Ответить"
   - Автоматически исчезает через 5 секунд
   - Позиция: top-right

2. Desktop notifications:
   - Запросить разрешение при первом входе
   - Показывать если вкладка неактивна
   - Клик по уведомлению открывает чат

3. Badge счетчик:
   - Показывать на иконке чата в навбаре
   - Общее количество непрочитанных
   - Обновлять real-time

4. Звуковой сигнал:
   - Короткий звук при новом сообщении
   - Настройка: включить/выключить в профиле

WebSocket обработка:
- Слушать событие message.new
- Проверить: открыт ли чат с этим собеседником
- Если нет - показать уведомление
- Обновить счетчик непрочитанных

Используй:
- react-hot-toast для toast уведомлений
- Web Notifications API для desktop
- howler.js для звука

Пример звука: короткий "ding" (1 секунда)
```

---

### 6️⃣ Групповые чаты (GroupChat)

**Отличия от личных:**
- Несколько участников
- Администратор группы (преподаватель)
- Возможность добавить/удалить участников
- Название группы можно изменить

**Промпт для разработки:**
```
Расширь ChatWindow.js для поддержки групповых чатов.

Дополнительные элементы для группового чата:

1. Header:
   - Название группы
   - Количество участников (например: "8 участников")
   - Кнопка "Участники" (открывает список)

2. Модальное окно "Участники":
   - Список всех участников группы
   - Аватар + имя + роль
   - Индикатор онлайн
   - Для админа: кнопка удалить участника
   - Для админа: кнопка добавить участника

3. Сообщения в группе:
   - Показывать имя отправителя над каждым сообщением
   - Разные цвета аватаров для разных людей
   - Упоминания @username подсвечиваются

4. Создание группового чата:
   - Модальное окно с формой
   - Название группы
   - Выбор участников (чекбоксы)
   - Кнопка "Создать"

API:
- POST /api/chat/conversations/group/
  {
      "name": "Математика 9 класс",
      "participants": [1, 2, 3, 4, 5]
  }
- POST /api/chat/conversations/{id}/add_participant/
- DELETE /api/chat/conversations/{id}/remove_participant/

Права доступа:
- Только преподаватель может создавать групповые чаты
- Только преподаватель может управлять участниками
- Все могут писать сообщения
```

---

## 🗂️ Backend требования

### Модели Django

```python
# chat/models.py

from django.db import models
from django.conf import settings

class Conversation(models.Model):
    """Чат (личный или групповой)"""
    CONVERSATION_TYPES = [
        ('DIRECT', 'Личный'),
        ('GROUP', 'Групповой'),
    ]
    
    type = models.CharField(max_length=10, choices=CONVERSATION_TYPES, default='DIRECT')
    name = models.CharField(max_length=255, blank=True)  # Для групповых
    participants = models.ManyToManyField(settings.AUTH_USER_MODEL, related_name='conversations')
    created_by = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True, related_name='created_conversations')
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        ordering = ['-updated_at']
    
    def __str__(self):
        return self.name or f"Conversation {self.id}"

class Message(models.Model):
    """Сообщение в чате"""
    MESSAGE_TYPES = [
        ('TEXT', 'Текст'),
        ('IMAGE', 'Изображение'),
        ('FILE', 'Файл'),
        ('SYSTEM', 'Системное'),
    ]
    
    conversation = models.ForeignKey(Conversation, on_delete=models.CASCADE, related_name='messages')
    sender = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE, related_name='sent_messages')
    message_type = models.CharField(max_length=10, choices=MESSAGE_TYPES, default='TEXT')
    text = models.TextField(blank=True)
    file = models.FileField(upload_to='chat_files/', blank=True, null=True)
    file_name = models.CharField(max_length=255, blank=True)
    file_size = models.IntegerField(null=True, blank=True)  # bytes
    mime_type = models.CharField(max_length=100, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    edited_at = models.DateTimeField(null=True, blank=True)
    
    class Meta:
        ordering = ['created_at']
    
    def __str__(self):
        return f"{self.sender.email}: {self.text[:50]}"

class MessageRead(models.Model):
    """Отметки о прочтении сообщений"""
    message = models.ForeignKey(Message, on_delete=models.CASCADE, related_name='read_by')
    user = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE)
    read_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        unique_together = ['message', 'user']

class UserOnlineStatus(models.Model):
    """Статус онлайн пользователей"""
    user = models.OneToOneField(settings.AUTH_USER_MODEL, on_delete=models.CASCADE, related_name='online_status')
    is_online = models.BooleanField(default=False)
    last_seen = models.DateTimeField(auto_now=True)
    
    def __str__(self):
        return f"{self.user.email} - {'Online' if self.is_online else 'Offline'}"
```

### WebSocket Consumer

```python
# chat/consumers.py

import json
from channels.generic.websocket import AsyncWebsocketConsumer
from channels.db import database_sync_to_async
from .models import Message, Conversation, MessageRead, UserOnlineStatus

class ChatConsumer(AsyncWebsocketConsumer):
    async def connect(self):
        self.conversation_id = self.scope['url_route']['kwargs']['conversation_id']
        self.room_group_name = f'chat_{self.conversation_id}'
        self.user = self.scope['user']
        
        # Проверить что пользователь участник чата
        is_participant = await self.check_participant()
        if not is_participant:
            await self.close()
            return
        
        # Подключиться к группе
        await self.channel_layer.group_add(
            self.room_group_name,
            self.channel_name
        )
        
        # Обновить статус онлайн
        await self.set_online_status(True)
        
        await self.accept()
    
    async def disconnect(self, close_code):
        # Покинуть группу
        await self.channel_layer.group_discard(
            self.room_group_name,
            self.channel_name
        )
        
        # Обновить статус оффлайн
        await self.set_online_status(False)
    
    async def receive(self, text_data):
        data = json.loads(text_data)
        message_type = data.get('type')
        
        if message_type == 'chat_message':
            message = await self.save_message(data['text'])
            
            # Отправить сообщение в группу
            await self.channel_layer.group_send(
                self.room_group_name,
                {
                    'type': 'chat_message',
                    'message': {
                        'id': message.id,
                        'text': message.text,
                        'sender': {
                            'id': self.user.id,
                            'name': f"{self.user.first_name} {self.user.last_name}",
                        },
                        'created_at': message.created_at.isoformat(),
                    }
                }
            )
        
        elif message_type == 'typing':
            # Уведомить других что пользователь печатает
            await self.channel_layer.group_send(
                self.room_group_name,
                {
                    'type': 'user_typing',
                    'user_id': self.user.id,
                    'is_typing': data['is_typing']
                }
            )
        
        elif message_type == 'mark_read':
            message_id = data['message_id']
            await self.mark_message_read(message_id)
    
    async def chat_message(self, event):
        # Отправить сообщение в WebSocket
        await self.send(text_data=json.dumps(event))
    
    async def user_typing(self, event):
        # Отправить индикатор печати
        if event['user_id'] != self.user.id:
            await self.send(text_data=json.dumps(event))
    
    @database_sync_to_async
    def check_participant(self):
        return Conversation.objects.filter(
            id=self.conversation_id,
            participants=self.user
        ).exists()
    
    @database_sync_to_async
    def save_message(self, text):
        conversation = Conversation.objects.get(id=self.conversation_id)
        return Message.objects.create(
            conversation=conversation,
            sender=self.user,
            message_type='TEXT',
            text=text
        )
    
    @database_sync_to_async
    def mark_message_read(self, message_id):
        message = Message.objects.get(id=message_id)
        MessageRead.objects.get_or_create(message=message, user=self.user)
    
    @database_sync_to_async
    def set_online_status(self, is_online):
        UserOnlineStatus.objects.update_or_create(
            user=self.user,
            defaults={'is_online': is_online}
        )
```

### WebSocket Routing

```python
# chat/routing.py

from django.urls import re_path
from . import consumers

websocket_urlpatterns = [
    re_path(r'ws/chat/(?P<conversation_id>\d+)/$', consumers.ChatConsumer.as_asgi()),
]
```

### REST API Views

```python
# chat/views.py

from rest_framework import viewsets, status
from rest_framework.decorators import action
from rest_framework.response import Response
from rest_framework.permissions import IsAuthenticated
from .models import Conversation, Message
from .serializers import ConversationSerializer, MessageSerializer

class ConversationViewSet(viewsets.ModelViewSet):
    permission_classes = [IsAuthenticated]
    serializer_class = ConversationSerializer
    
    def get_queryset(self):
        return Conversation.objects.filter(participants=self.request.user)
    
    @action(detail=False, methods=['post'])
    def create_group(self, request):
        """Создать групповой чат"""
        name = request.data.get('name')
        participant_ids = request.data.get('participants', [])
        
        conversation = Conversation.objects.create(
            type='GROUP',
            name=name,
            created_by=request.user
        )
        conversation.participants.add(request.user, *participant_ids)
        
        serializer = self.get_serializer(conversation)
        return Response(serializer.data, status=status.HTTP_201_CREATED)
    
    @action(detail=True, methods=['post'])
    def add_participant(self, request, pk=None):
        """Добавить участника в групповой чат"""
        conversation = self.get_object()
        
        # Проверить права (только создатель может добавлять)
        if conversation.created_by != request.user:
            return Response({'error': 'Permission denied'}, status=status.HTTP_403_FORBIDDEN)
        
        user_id = request.data.get('user_id')
        conversation.participants.add(user_id)
        
        return Response({'status': 'participant added'})
    
    @action(detail=True, methods=['post'])
    def remove_participant(self, request, pk=None):
        """Удалить участника из группового чата"""
        conversation = self.get_object()
        
        if conversation.created_by != request.user:
            return Response({'error': 'Permission denied'}, status=status.HTTP_403_FORBIDDEN)
        
        user_id = request.data.get('user_id')
        conversation.participants.remove(user_id)
        
        return Response({'status': 'participant removed'})

class MessageViewSet(viewsets.ModelViewSet):
    permission_classes = [IsAuthenticated]
    serializer_class = MessageSerializer
    
    def get_queryset(self):
        conversation_id = self.request.query_params.get('conversation_id')
        return Message.objects.filter(
            conversation_id=conversation_id,
            conversation__participants=self.request.user
        )
    
    def perform_create(self, serializer):
        serializer.save(sender=self.request.user)
```

---

## 📦 Структура файлов модуля

```
frontend/src/modules/chat/
├── README.md
├── components/
│   ├── ChatList.js              # Список чатов
│   ├── ChatWindow.js            # Окно чата
│   ├── MessageInput.js          # Поле ввода
│   ├── MessageItem.js           # Элемент сообщения
│   ├── FileUpload.js            # Загрузка файлов
│   ├── ChatNotifications.js     # Уведомления
│   ├── GroupChatModal.js        # Модалка создания группы
│   └── ParticipantsList.js      # Список участников
├── services/
│   ├── chatService.js           # REST API
│   └── websocketService.js      # WebSocket клиент
├── hooks/
│   ├── useChat.js               # Хук для работы с чатом
│   ├── useWebSocket.js          # Хук для WebSocket
│   └── useNotifications.js      # Хук для уведомлений
└── context/
    └── ChatContext.js           # Глобальное состояние чатов

teaching_panel/chat/             # Backend
├── __init__.py
├── models.py                    # Модели БД
├── serializers.py               # DRF сериализаторы
├── views.py                     # REST API views
├── consumers.py                 # WebSocket consumer
├── routing.py                   # WebSocket routing
├── admin.py                     # Django admin
└── migrations/
```

---

## ✅ Чеклист разработки

### Этап 1: Backend основа (2-3 дня)
- [ ] Создать Django app `chat`
- [ ] Модели: Conversation, Message, MessageRead, UserOnlineStatus
- [ ] Миграции БД
- [ ] Настроить Django Channels
- [ ] Настроить Redis
- [ ] WebSocket Consumer базовый
- [ ] REST API для чатов и сообщений

### Этап 2: Frontend основа (2-3 дня)
- [ ] ChatContext для глобального состояния
- [ ] websocketService.js для подключения
- [ ] chatService.js для REST API
- [ ] ChatList.js базовая версия
- [ ] ChatWindow.js базовая версия

### Этап 3: Real-time функционал (2-3 дня)
- [ ] WebSocket отправка/получение сообщений
- [ ] Обновление списка чатов real-time
- [ ] Индикатор "печатает..."
- [ ] Статусы онлайн/оффлайн
- [ ] Отметки о прочтении

### Этап 4: Файлы и медиа (2 дня)
- [ ] Endpoint загрузки файлов
- [ ] FileUpload.js с drag & drop
- [ ] Сжатие изображений
- [ ] Превью файлов в чате
- [ ] Скачивание файлов

### Этап 5: Уведомления (1-2 дня)
- [ ] Toast уведомления
- [ ] Desktop notifications
- [ ] Badge счетчик
- [ ] Звуковые сигналы
- [ ] Настройки уведомлений

### Этап 6: Групповые чаты (2-3 дня)
- [ ] Создание группового чата
- [ ] Управление участниками
- [ ] Отображение имени отправителя
- [ ] Упоминания @username

### Этап 7: UX улучшения (2 дня)
- [ ] Поиск по чатам
- [ ] Фильтры (непрочитанные, группы)
- [ ] Эмодзи пикер
- [ ] Markdown форматирование
- [ ] Адаптивность для мобильных

### Этап 8: Тестирование (2 дня)
- [ ] Unit тесты для компонентов
- [ ] Integration тесты WebSocket
- [ ] Тестирование на разных браузерах
- [ ] Нагрузочное тестирование

---

## 🚀 Порядок промптов для работы

### День 1-3: Backend
```
1. "Создай Django app chat с моделями Conversation, Message..."
2. "Настрой Django Channels и Redis для WebSocket..."
3. "Создай WebSocket Consumer для чата с методами connect/disconnect/receive..."
4. "Создай REST API ViewSets для чатов и сообщений..."
```

### День 4-6: Frontend основа
```
5. "Создай ChatContext.js для глобального состояния чатов..."
6. "Создай websocketService.js для подключения к WebSocket..."
7. "Создай chatService.js с методами для REST API чатов..."
8. "Создай ChatList.js для списка чатов с аватарами и превью..."
9. "Создай ChatWindow.js для окна переписки..."
```

### День 7-9: Real-time
```
10. "Добавь real-time отправку/получение сообщений через WebSocket..."
11. "Реализуй индикатор 'печатает...' с debounce..."
12. "Добавь статусы онлайн/оффлайн для пользователей..."
13. "Реализуй отметки о прочтении сообщений (✓✓)..."
```

### День 10-11: Файлы
```
14. "Создай endpoint для загрузки файлов в чат..."
15. "Создай FileUpload.js с drag-and-drop и превью..."
16. "Добавь сжатие изображений на клиенте перед загрузкой..."
17. "Реализуй отображение файлов в ChatWindow..."
```

### День 12-13: Уведомления
```
18. "Создай ChatNotifications.js с toast уведомлениями..."
19. "Добавь Desktop notifications с запросом разрешения..."
20. "Реализуй badge счетчик непрочитанных в навбаре..."
21. "Добавь звуковой сигнал при новом сообщении..."
```

### День 14-16: Групповые чаты
```
22. "Добавь поддержку групповых чатов в модели и API..."
23. "Создай GroupChatModal.js для создания групповых чатов..."
24. "Реализуй управление участниками группы..."
25. "Добавь отображение имени отправителя в групповых чатах..."
26. "Реализуй упоминания @username с автодополнением..."
```

### День 17-18: UX
```
27. "Добавь поиск по чатам и сообщениям..."
28. "Реализуй фильтры: Все / Непрочитанные / Группы..."
29. "Добавь emoji-picker для выбора эмодзи..."
30. "Реализуй markdown форматирование текста..."
```

### День 19-20: Тестирование
```
31. "Напиши unit тесты для ChatList и ChatWindow..."
32. "Протестируй WebSocket на разрыв соединения..."
33. "Проверь адаптивность на мобильных..."
```

---

## 🎁 Бонусные фичи

1. **Голосовые сообщения** - запись и отправка аудио
2. **Видеозвонки** - интеграция WebRTC
3. **Пересылка сообщений** - forward в другой чат
4. **Реакции на сообщения** - эмодзи реакции
5. **Редактирование сообщений** - исправить отправленное
6. **Закрепленные сообщения** - pin важные сообщения
7. **Поиск по истории** - полнотекстовый поиск
8. **Экспорт истории** - скачать переписку в PDF

**Удачи! 🚀**
