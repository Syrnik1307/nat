# 🧪 Тестирование автодеплой скрипта

## ✅ Чек-лист перед использованием

### Предварительные требования

- [ ] Windows 10/11 с PowerShell 5.1+
- [ ] SSH клиент установлен
- [ ] SSH ключи настроены
- [ ] Доступ к серверу 72.56.81.163
- [ ] Git установлен (для локальных коммитов)

## 🔧 Проверка SSH

### 1. Проверить наличие SSH клиента

```powershell
ssh -V
```

**Ожидаемый результат:**
```
OpenSSH_for_Windows_8.x, LibreSSL x.x
```

**Если команда не найдена:**
```powershell
winget install Microsoft.OpenSSH.Beta
```

### 2. Проверить SSH ключи

```powershell
# Проверить наличие ключей
Test-Path $env:USERPROFILE\.ssh\id_rsa.pub

# Если False, создать ключ
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"
```

### 3. Проверить подключение к серверу

```powershell
ssh root@72.56.81.163 "echo 'SSH works!'"
```

**Ожидаемый результат:**
```
SSH works!
```

**Если ошибка "Permission denied":**
```powershell
# Скопировать ключ на сервер
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@72.56.81.163 "cat >> ~/.ssh/authorized_keys"
```

## 🚀 Тестирование скрипта

### Тест 1: Запуск скрипта

```powershell
cd C:\Users\User\Desktop\nat
.\auto_deploy.ps1
```

**Ожидаемый результат:**
- ✅ Скрипт запускается
- ✅ Показывается проверка SSH подключения
- ✅ Выводится меню с опциями

**Скриншот ожидаемого вывода:**
```
ℹ️ Проверка подключения к серверу...
✅ SSH соединение установлено

╔════════════════════════════════════════════╗
║   Teaching Panel - Автодеплой на сервер   ║
╚════════════════════════════════════════════╝

Сервер: root@72.56.81.163
Путь: /var/www/teaching_panel

╔════════════════════════════════════════════╗
║ ОСНОВНЫЕ ДЕЙСТВИЯ                          ║
╠════════════════════════════════════════════╣
║ 1 - 🚀 Полный деплой (всё)                 ║
...
```

### Тест 2: Проверка статуса (безопасная операция)

```
Выбрать: 6 (Статус сервисов)
```

**Ожидаемый результат:**
- ✅ Показывается статус всех сервисов
- ✅ Видны: teaching_panel, celery, celery-beat, nginx, redis-server
- ✅ Статусы: active (running) или failed

### Тест 3: Просмотр логов (безопасная операция)

```
Выбрать: 7 (Просмотр логов)
Затем: 1 (Django логи)
```

**Ожидаемый результат:**
- ✅ Показываются последние 100 строк логов Django
- ✅ Видны записи с датой и временем
- ✅ Нет критических ошибок (ERROR/CRITICAL)

### Тест 4: Проверка здоровья системы (безопасная операция)

```
Выбрать: 8 (Проверить здоровье системы)
```

**Ожидаемый результат:**
- ✅ Показывается использование диска
- ✅ Показывается использование памяти
- ✅ Показываются Python процессы
- ✅ Показываются статусы сервисов
- ✅ Показываются последние ошибки (если есть)

### Тест 5: Перезапуск сервисов (безопасная операция)

```
Выбрать: 5 (Перезапустить сервисы)
```

**Ожидаемый результат:**
- ✅ Сервисы перезапускаются без ошибок
- ✅ Показывается статус: "Перезапуск сервисов - Готово"
- ✅ Сайт остаётся доступным после перезапуска

**Проверка после перезапуска:**
```powershell
# Открыть браузер
Start-Process "http://72.56.81.163"

# Или проверить curl'ом
Invoke-WebRequest -Uri "http://72.56.81.163/api/" -UseBasicParsing
```

### Тест 6: Применение миграций (относительно безопасная операция)

```
Выбрать: 4 (Применить миграции БД)
```

**Ожидаемый результат:**
- ✅ Миграции применяются
- ✅ Если нет новых миграций: "No migrations to apply"
- ✅ Если есть миграции: "Applying <app>.<migration>... OK"

### Тест 7: Обновление только фронтенда

**Предварительно:**
1. Сделать небольшое изменение в React коде (например, комментарий)
2. Закоммитить и запушить в Git

```
Выбрать: 3 (Обновить только фронтенд)
```

**Ожидаемый результат:**
- ✅ git pull выполняется успешно
- ✅ npm install завершается без ошибок
- ✅ npm run build собирает проект
- ✅ Nginx перезапускается
- ✅ Изменения видны на сайте (очистить кеш браузера: Ctrl+Shift+R)

**Время выполнения:** ~1-2 минуты

### Тест 8: Обновление только бэкенда

**Предварительно:**
1. Сделать небольшое изменение в Django коде (например, комментарий)
2. Закоммитить и запушить в Git

```
Выбрать: 2 (Обновить только бэкенд)
```

**Ожидаемый результат:**
- ✅ git pull выполняется успешно
- ✅ pip install завершается без ошибок
- ✅ migrate применяет миграции (если есть)
- ✅ collectstatic собирает статику
- ✅ Django и Celery перезапускаются
- ✅ API работает корректно

**Время выполнения:** ~1-2 минуты

### Тест 9: Полный деплой

**Предварительно:**
1. Сделать изменения в коде (и фронтенд, и бэкенд)
2. Закоммитить и запушить в Git

```
Выбрать: 1 (Полный деплой)
```

**Ожидаемый результат:**
- ✅ Все 8 шагов выполняются последовательно:
  1. git pull
  2. pip install
  3. migrate
  4. collectstatic
  5. npm install
  6. npm run build
  7. restart Django/Celery
  8. restart Nginx
- ✅ Каждый шаг помечен как "Готово"
- ✅ В конце: "ДЕПЛОЙ ЗАВЕРШЁН УСПЕШНО!"
- ✅ Сайт доступен и работает корректно

**Время выполнения:** ~3-5 минут

## 🧹 Тесты обслуживания

### Тест 10: Очистка кеша

```
Выбрать: 9 (Очистить кеш и temp файлы)
```

**Ожидаемый результат:**
- ✅ Удаляются __pycache__ директории
- ✅ Удаляются .pyc файлы
- ✅ Очищается npm cache
- ✅ Очищается Django cache (если настроен)
- ✅ Сообщение: "Очистка завершена!"

### Тест 11: Обновление зависимостей

```
Выбрать: 10 (Обновить зависимости)
```

**Ожидаемый результат:**
- ✅ pip обновляется
- ✅ Python пакеты обновляются
- ✅ npm пакеты обновляются
- ✅ Сообщение: "Зависимости обновлены!"

**Внимание:** Может занять 5-10 минут

### Тест 12: Проверка SSL

```
Выбрать: 11 (Проверить SSL сертификат)
```

**Ожидаемый результат:**
- ✅ Показывается информация о сертификате
- ✅ Виден срок действия
- ✅ Виден домен

## ⚠️ Тестирование ошибочных ситуаций

### Тест 13: SSH недоступен

```powershell
# Временно запретить SSH
# (на сервере): sudo ufw deny 22/tcp

# Запустить скрипт
.\auto_deploy.ps1
```

**Ожидаемый результат:**
- ✅ Скрипт обнаруживает проблему с SSH
- ✅ Показывает информативное сообщение об ошибке
- ✅ Предлагает решения
- ✅ Корректно завершается (не зависает)

**Восстановить:**
```bash
# (на сервере через консоль): sudo ufw allow 22/tcp
```

### Тест 14: Сервер недоступен

```powershell
# Временно изменить IP в скрипте на несуществующий
# Например: $SERVER = "root@1.2.3.4"

# Запустить скрипт
.\auto_deploy.ps1
```

**Ожидаемый результат:**
- ✅ Скрипт обнаруживает недоступность сервера
- ✅ Показывает таймаут ошибки
- ✅ Не зависает надолго (максимум 5 секунд)

## 📊 Критерии успешного теста

### Функциональность
- [ ] Все 12 основных тестов пройдены успешно
- [ ] Скрипт корректно обрабатывает ошибки
- [ ] Время выполнения соответствует ожиданиям
- [ ] Сайт остаётся доступным во время деплоя

### Пользовательский опыт
- [ ] Меню понятное и интуитивное
- [ ] Цветной вывод помогает понять статус
- [ ] Сообщения об ошибках информативные
- [ ] Скрипт не требует постоянного внимания

### Безопасность
- [ ] SSH ключи проверяются корректно
- [ ] Нет утечки паролей или ключей
- [ ] Критические операции можно отменить
- [ ] Логи не содержат чувствительных данных

## 🐛 Если тест провалился

### Шаг 1: Проверить SSH
```powershell
ssh root@72.56.81.163 "echo OK"
```

### Шаг 2: Проверить доступность сервера
```powershell
Test-NetConnection -ComputerName 72.56.81.163 -Port 22
```

### Шаг 3: Проверить логи на сервере
```powershell
ssh root@72.56.81.163 "sudo journalctl -u teaching_panel -n 50 --no-pager"
```

### Шаг 4: Откатить изменения (если нужно)
```powershell
ssh root@72.56.81.163 "cd /var/www/teaching_panel && git reset --hard HEAD~1"
```

### Шаг 5: Перезапустить сервисы вручную
```powershell
ssh root@72.56.81.163 "sudo systemctl restart teaching_panel celery celery-beat nginx"
```

## ✅ Итоговый чек-лист

После прохождения всех тестов:

- [ ] Скрипт работает стабильно
- [ ] SSH подключение настроено
- [ ] Деплой выполняется за ожидаемое время
- [ ] Сайт доступен после деплоя
- [ ] Ошибки обрабатываются корректно
- [ ] Документация понятна и актуальна

---

**Статус**: ✅ Готов к продакшн использованию  
**Дата тестирования**: 29 ноября 2025 г.
