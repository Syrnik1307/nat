# 🚀 Шпаргалка по deploy_unified.ps1

## Самые частые команды

```powershell
# Полный деплой (используй в 90% случаев)
.\deploy_unified.ps1 -Action full

# Интерактивное меню
.\deploy_unified.ps1

# Быстрый рестарт (применить config без изменений кода)
.\deploy_unified.ps1 -Action quick

# Только backend hotfix
.\deploy_unified.ps1 -Action backend

# Только frontend hotfix (сначала npm run build!)
.\deploy_unified.ps1 -Action frontend
```

## Что делает скрипт автоматически:

✅ Создаёт бэкап БД  
✅ Проверяет изменения в Git  
✅ Спрашивает подтверждение  
✅ Применяет изменения  
✅ Проверяет миграции  
✅ Перезапускает сервисы  
✅ Делает health check  
✅ **ОТКАТЫВАЕТСЯ при любой ошибке!**

## В случае проблем:

```powershell
# Посмотреть статус
.\deploy_unified.ps1
# → Выбрать "6 - Статус сервисов"

# Посмотреть логи
.\deploy_unified.ps1
# → Выбрать "7 - Просмотр логов" → "1 - Django error log"

# Ручной откат (если скрипт не справился)
ssh tp
cd /var/www/teaching_panel
sudo git reset --hard <commit-hash>
sudo cp /tmp/deploy_YYYYMMDD_HHMMSS.sqlite3 teaching_panel/db.sqlite3
sudo systemctl restart teaching_panel
```

## Полная документация:
Смотри `DEPLOY_UNIFIED_README.md`
