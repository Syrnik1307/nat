# 🛡️ Документация по тестированию админ-панели LMS Teaching Panel

**Дата:** 2 декабря 2025  
**Статус:** Актуально  
**Покрытие:** Все функции администрирования, автотесты, ручные проверки

---

## 1. Общая архитектура админ-панели

- **Backend:** Django + DRF, все административные API в `accounts/admin_views.py`
- **Frontend:** React, компонент `AdminHomePage.js` + модальные окна управления (`TeachersManage.js`, `StudentsManage.js`, `StorageQuotaModal.js`, `SubscriptionsModal.js`, `ZoomPoolManager.js`)
- **Права доступа:** Только пользователи с `role=admin`, `is_staff=True` или `is_superuser=True`
- **Админ-интерфейс Django:** `/admin/` для прямого доступа к моделям

---

## 2. Список функций для проверки

### 2.1. Статистика и мониторинг
- **API:** `/accounts/api/admin/stats/`
- **Проверить:** Общее число пользователей, учителей, учеников, онлайн-статус, группы, уроки, Zoom-аккаунты, рост за периоды
- **Автотест:** Проверить корректность подсчёта, права доступа (403 для не-админов)

### 2.2. Управление учителями
- **API:** `/accounts/api/admin/teachers/` (список), `/accounts/api/admin/teachers/<id>/` (детали)
- **Проверить:** Поиск, фильтрация, просмотр профиля, метрики (уроки, группы, ученики, Zoom-настройки)
- **Автотест:** Создание, удаление, обновление, права доступа

### 2.3. Управление учениками
- **API:** `/accounts/api/admin/students/`, `/accounts/api/admin/students/<id>/update/`, `/accounts/api/admin/students/<id>/delete/`
- **Проверить:** Поиск, фильтрация, просмотр профиля, удаление, обновление
- **Автотест:** Проверить невозможность удаления учителя через student API

### 2.4. Управление Zoom-аккаунтами
- **API:** `/api/zoom-accounts/`, `/api/zoom-accounts/<id>/release/`, `/api/zoom-accounts/usage-stats/`
- **Проверить:** Добавление, удаление, редактирование, просмотр статистики, ручной релиз аккаунта
- **Автотест:** Проверить права доступа (только admin/teacher), корректность статистики

### 2.5. Управление квотами хранения
- **API:** `/storage/quotas/`, `/storage/quotas/<id>/increase/`, `/storage/quotas/<id>/reset-warnings/`
- **Проверить:** Просмотр квот, увеличение квоты, сброс предупреждений, фильтры, поиск
- **Автотест:** Проверить невозможность увеличения квоты без прав

### 2.6. Управление подписками
- **API:** `/subscription/`, `subscription/cancel/`
- **Проверить:** Просмотр всех подписок, фильтрация по статусу/тарифу, отмена автопродления, просмотр истории платежей
- **Автотест:** Проверить невозможность отмены чужой подписки

### 2.7. Системные настройки
- **API:** `/accounts/api/admin/settings/`
- **Проверить:** Просмотр и изменение глобальных настроек (например, лимиты, параметры интеграций)
- **Автотест:** Проверить права доступа, валидацию

### 2.8. Сообщения статуса
- **API:** `/accounts/api/admin/status-messages/`
- **Проверить:** Добавление, удаление, просмотр сообщений для пользователей
- **Автотест:** Проверить корректность отображения и удаления

### 2.9. Запуск и мониторинг Celery задач
- **API:** `/api/celery/metrics/`, `/api/celery/trigger/<task_name>/`
- **Проверить:** Просмотр состояния воркеров, запуск задач вручную, просмотр истории запусков
- **Автотест:** Проверить права доступа, корректность запуска

---

## 3. Чек-лист ручного тестирования

1. **Вход под админом:**  
   - Создать суперпользователя: `python manage.py createsuperuser`
   - Проверить вход в `/admin/` и в SPA

2. **Проверка всех разделов SPA:**  
   - Статистика (главная страница)
   - Управление учителями/учениками (поиск, фильтры, действия)
   - Zoom-аккаунты (добавить, удалить, релиз)
   - Квоты хранения (увеличить, сбросить предупреждения)
   - Подписки (отмена, просмотр истории)
   - Системные настройки (изменить, сохранить)
   - Сообщения статуса (добавить, удалить)
   - Celery (статус, запуск задач)

3. **Проверка прав доступа:**  
   - Попробовать все API под обычным учителем/учеником (должно быть 403)
   - Проверить, что только админ может видеть и изменять системные настройки

4. **Проверка интеграций:**  
   - Zoom: добавить/удалить аккаунт, проверить статистику
   - Хранилище: увеличить квоту, сбросить предупреждения
   - Подписки: отменить, проверить историю платежей

5. **Проверка ошибок и edge cases:**  
   - Попробовать удалить несуществующего пользователя
   - Попробовать увеличить квоту до абсурдного значения
   - Попробовать отменить подписку дважды

---

## 4. Автотесты (unit/integration)

- **Backend:**  
  - `accounts/tests/test_admin_views.py` — тесты на все admin API
  - `zoom_pool/tests/test_admin_zoom.py` — тесты на ZoomPoolManager
  - `schedule/tests/test_celery_metrics.py` — тесты на celery_metrics
  - Проверить покрытие: `python manage.py test accounts zoom_pool schedule`

- **Frontend:**  
  - `src/components/AdminHomePage.test.js` — smoke-тесты на все разделы
  - `src/components/TeachersManage.test.js` — тесты на CRUD учителей
  - `src/modules/Admin/StorageQuotaModal.test.js` — тесты на квоты
  - Проверить покрытие: `npm test`

---

## 5. Метрики успеха

| Функция                | Целевое значение         | Как проверить           |
|------------------------|-------------------------|------------------------|
| Время ответа API       | < 500 мс                | DevTools, Postman      |
| Корректность прав      | 100%                    | Ручные/автотесты       |
| Успешность операций    | > 99%                   | Логи, тесты            |
| Покрытие автотестами   | > 90%                   | Coverage report        |

---

## 6. Рекомендации по улучшению

- Добавить массовое создание/удаление пользователей
- Ввести логи действий админа
- Реализовать экспорт/импорт данных
- Добавить интеграцию с Telegram/email для уведомлений

---

**Документ готов для полного аудита админ-панели. Все функции покрыты, автотесты и ручные проверки описаны.**
