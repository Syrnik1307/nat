# Root Cause Analysis: Пропажа изображений в домашних заданиях

**Дата:** 2026-02-01  
**Статус:** CRITICAL - Требует немедленного исправления  
**Затронутый кейс:** ДЗ от 14.12 для группы "Информатика 1"

---

## 1. Резюме

Изображения в домашних заданиях периодически пропадают (отображаются как битые ссылки). 
Причина — **агрессивные cleanup-скрипты**, которые удаляют папки `Teacher_*` на Google Drive 
**без проверки наличия активных файлов ДЗ** в этих папках.

---

## 2. Архитектура хранения изображений ДЗ

### Модель данных

```
Question.config = {
    "imageUrl": "/api/homework/file/<id>/",      # URL для фронтенда
    "imageFileId": "<homework_file_id>"          # ID записи HomeworkFile
}

HomeworkFile (models.py:724)
├── id (PK, UUID-like)
├── teacher (FK → CustomUser)
├── original_name
├── mime_type, size
├── storage: "local" | "gdrive"
├── local_path (путь к локальному файлу)
├── gdrive_file_id (ID файла на Google Drive)    ← КЛЮЧЕВОЕ ПОЛЕ
└── gdrive_url (direct download URL)
```

### Путь файла

1. **Загрузка** → файл сохраняется локально в `MEDIA_ROOT/homework_files/`
2. **Миграция** → cron job `migrate_homework_files` переносит на GDrive
3. **Структура GDrive:**
   ```
   lectio.space (GDRIVE_ROOT_FOLDER_ID)
   └── Teacher_<email>/
       └── Homework/
           └── Uploads/
               └── hw_<file_id>_<original_name>
   ```

### Прокси-доступ

Фронтенд запрашивает `/api/homework/file/<id>/` → `HomeworkFileProxyView`:
1. Проверяет `local_path` → отдаёт если существует
2. Проверяет кэш `MEDIA_ROOT/homework_cache/` → отдаёт если есть
3. Скачивает с GDrive по `gdrive_file_id` → кэширует и отдаёт

---

## 3. Корневые причины (Root Causes)

### RC1: Опасный скрипт `cleanup_lectio_folders.py` 🚨

**Файл:** `teaching_panel/cleanup_lectio_folders.py`

```python
# КРИТИЧЕСКИ ОПАСНЫЙ КОД:
query = f"'{root_id}' in parents and mimeType='application/vnd.google-apps.folder' and name contains 'Teacher_' and trashed=false"
for f in folders:
    gdrive.service.files().delete(fileId=f['id']).execute()  # УДАЛЯЕТ ВСЕ!
```

**Проблема:** Скрипт удаляет ВСЕ папки `Teacher_*` без проверки:
- Содержат ли они активные файлы ДЗ
- Используются ли в `HomeworkFile.gdrive_file_id`
- Привязаны ли к активным подпискам

### RC2: Опасный скрипт `cleanup_teacher_folders.py` 🚨

**Файл:** `teaching_panel/cleanup_teacher_folders.py`

Аналогичная проблема — запрашивает подтверждение, но всё равно удаляет папки целиком
с содержимым, не проверяя что внутри.

### RC3: Скрипт `cleanup_old_gdrive_folders.py` (частично безопасен)

**Файл:** `teaching_panel/cleanup_old_gdrive_folders.py`

Этот скрипт проверяет привязку к подпискам (`Subscription.gdrive_folder_id`), 
но **НЕ ПРОВЕРЯЕТ** `HomeworkFile.gdrive_file_id`.

### RC4: Нет защиты от удаления активных файлов

- `HomeworkFile` хранит только `gdrive_file_id` конкретного файла
- При удалении папки учителя файлы внутри тоже удаляются
- Но `HomeworkFile` записи остаются, ссылаясь на несуществующие файлы

### RC5: Нет валидации при отображении

Фронтенд не обрабатывает 404 от прокси корректно — показывает битую картинку
вместо placeholder или повторной попытки загрузки.

---

## 4. Затронутые файлы

| Файл | Риск | Описание |
|------|------|----------|
| `cleanup_lectio_folders.py` | 🔴 CRITICAL | Удаляет ВСЕ без проверки |
| `cleanup_teacher_folders.py` | 🔴 CRITICAL | Удаляет ВСЕ с подтверждением |
| `cleanup_old_gdrive_folders.py` | 🟡 MEDIUM | Проверяет подписки, но не файлы ДЗ |
| `cleanup_local_homework_files.py` | 🟢 SAFE | Удаляет только мигрированные локальные |

---

## 5. План исправления

### Немедленно (P0)

1. **Заблокировать опасные скрипты** — добавить SAFETY_LOCK
2. **Создать скрипт восстановления** — `diagnose_and_restore_hw_images.py` ✅ DONE
3. **Восстановить файлы из корзины GDrive** (хранятся 30 дней)

### Краткосрочно (P1)

4. **Добавить проверку HomeworkFile** в cleanup-скрипты
5. **Добавить integrity check** перед миграцией
6. **Frontend: обработка 404** — показывать placeholder

### Долгосрочно (P2)

7. **Soft delete** для файлов GDrive (trash вместо delete)
8. **Reference counting** — не удалять папку пока есть ссылки
9. **Backup policy** — ежедневные снапшоты metadata
10. **Миграция на S3/Azure Blob** — более надёжное хранилище

---

## 6. Инструкции по восстановлению

### Шаг 1: Диагностика

```bash
cd teaching_panel
source ../venv/bin/activate
python ../diagnose_and_restore_hw_images.py --diagnose --group "Информатика 1" --date 2024-12-14
```

### Шаг 2: Восстановление с бэкапом

```bash
python ../diagnose_and_restore_hw_images.py --restore --backup --group "Информатика 1" --date 2024-12-14
```

### Шаг 3: Проверка

```bash
python ../diagnose_and_restore_hw_images.py --diagnose --homework-id <HW_ID> -v
```

---

## 7. Заключение

**Основная причина:** cleanup-скрипты удаляют папки `Teacher_*` без проверки содержимого,
что приводит к потере файлов ДЗ, на которые ссылается `HomeworkFile`.

**Решение:** 
1. Исправить cleanup-скрипты с добавлением проверки `HomeworkFile`
2. Восстановить потерянные файлы из корзины GDrive
3. Добавить защитные механизмы на уровне архитектуры

---

*Автор: AI Backend Engineer*  
*Время анализа: ~30 минут*
