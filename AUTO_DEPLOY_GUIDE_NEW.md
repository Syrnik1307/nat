# 🚀 АВТОМАТИЧЕСКИЙ ДЕПЛОЙ - QUICK START

## ✅ Два варианта деплоя

### Вариант 1: Полный деплой на новый сервер (первая установка)
Используйте **Bash скрипт** на сервере: `teaching_panel/deployment/deploy.sh`

### Вариант 2: Быстрое обновление существующего сервера ⭐ **РЕКОМЕНДУЕТСЯ**
Используйте **PowerShell скрипт** с Windows: `auto_deploy.ps1`

---

## ⭐ Вариант 2: Быстрый деплой с Windows (auto_deploy.ps1)

### 🎯 Для кого этот скрипт?

Для разработчиков на **Windows**, которые хотят быстро деплоить изменения на **уже настроенный** сервер.

### ✅ Преимущества:

- 🚀 **Интерактивное меню** - выбираете что обновить
- 🔍 **Мониторинг** - проверка статуса и логов в 1 клик
- 🧹 **Обслуживание** - очистка кеша, обновление зависимостей
- ⚡ **Быстро** - деплой за 2-3 минуты вместо 30+
- 🛡️ **Безопасно** - проверка SSH перед выполнением

### 📋 Предварительные требования:

1. **SSH клиент установлен** (обычно уже есть в Windows 10/11)
2. **SSH ключи настроены** для подключения к серверу
3. **Сервер уже настроен** (через deploy.sh или вручную)

### 🔧 Настройка SSH (если ещё не настроено):

#### Шаг 1: Проверить наличие SSH
```powershell
ssh -V
# Если команда не найдена, установите:
winget install Microsoft.OpenSSH.Beta
```

#### Шаг 2: Создать SSH ключ
```powershell
# Генерируем ключ (если его нет)
ssh-keygen -t rsa -b 4096 -C "ваш-email@example.com"
# Нажимайте Enter на все вопросы (использовать значения по умолчанию)
```

#### Шаг 3: Скопировать ключ на сервер
```powershell
# Копируем публичный ключ
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@72.56.81.163 "cat >> ~/.ssh/authorized_keys"

# Или вручную:
# 1. Скопировать содержимое файла C:\Users\Ваше_Имя\.ssh\id_rsa.pub
# 2. На сервере выполнить:
#    nano ~/.ssh/authorized_keys
#    Вставить ключ и сохранить (Ctrl+O, Enter, Ctrl+X)
```

#### Шаг 4: Проверить подключение
```powershell
ssh root@72.56.81.163 "echo 'SSH работает!'"
# Должно вывести: SSH работает!
```

### 🚀 Запуск скрипта:

```powershell
# Перейти в директорию проекта
cd C:\Users\User\Desktop\nat

# Запустить скрипт
.\auto_deploy.ps1
```

### 📋 Меню автодеплоя:

```
╔════════════════════════════════════════════╗
║   Teaching Panel - Автодеплой на сервер   ║
╚════════════════════════════════════════════╝

╔════════════════════════════════════════════╗
║ ОСНОВНЫЕ ДЕЙСТВИЯ                          ║
╠════════════════════════════════════════════╣
║ 1 - 🚀 Полный деплой (всё)                 ║
║ 2 - 🐍 Обновить только бэкенд (Django)     ║
║ 3 - ⚛️  Обновить только фронтенд (React)   ║
║ 4 - 🗄️  Применить миграции БД              ║
║ 5 - 🔄 Перезапустить сервисы               ║
╠════════════════════════════════════════════╣
║ МОНИТОРИНГ                                 ║
╠════════════════════════════════════════════╣
║ 6 - 📊 Статус сервисов                     ║
║ 7 - 📋 Просмотр логов                      ║
║ 8 - 🔍 Проверить здоровье системы          ║
╠════════════════════════════════════════════╣
║ ОБСЛУЖИВАНИЕ                               ║
╠════════════════════════════════════════════╣
║ 9 - 🧹 Очистить кеш и temp файлы           ║
║ 10 - 📦 Обновить зависимости               ║
║ 11 - 🔐 Проверить SSL сертификат           ║
╠════════════════════════════════════════════╣
║ 0 - 👋 Выход                               ║
╚════════════════════════════════════════════╝
```

### 🎯 Типичные сценарии использования:

#### Сценарий 1: Обновил код - нужен полный деплой
```
Выбрать: 1 (Полный деплой)
Время: ~3-5 минут
Что делает:
  ✅ git pull
  ✅ pip install
  ✅ python manage.py migrate
  ✅ collectstatic
  ✅ npm install
  ✅ npm run build
  ✅ restart всех сервисов
```

#### Сценарий 2: Изменил только бэкенд (Python/Django)
```
Выбрать: 2 (Обновить только бэкенд)
Время: ~1-2 минуты
Что делает:
  ✅ git pull
  ✅ pip install
  ✅ migrate
  ✅ collectstatic
  ✅ restart Django + Celery
```

#### Сценарий 3: Изменил только фронтенд (React)
```
Выбрать: 3 (Обновить только фронтенд)
Время: ~1-2 минуты
Что делает:
  ✅ git pull
  ✅ npm install
  ✅ npm run build
  ✅ restart Nginx
```

#### Сценарий 4: Добавил миграции БД
```
Выбрать: 4 (Применить миграции БД)
Время: ~10-30 секунд
Что делает:
  ✅ python manage.py migrate
```

#### Сценарий 5: Что-то сломалось - перезапустить
```
Выбрать: 5 (Перезапустить сервисы)
Время: ~5-10 секунд
Что делает:
  ✅ restart Django, Celery, Nginx, Redis
```

#### Сценарий 6: Проверить что всё работает
```
Выбрать: 6 (Статус сервисов)
Показывает:
  ✅ Статус всех сервисов (active/failed)
```

#### Сценарий 7: Посмотреть что пошло не так
```
Выбрать: 7 (Просмотр логов)
Затем выбрать:
  1 - Django логи
  2 - Celery логи
  3 - Nginx access логи
  4 - Nginx error логи
  5 - Redis логи
```

#### Сценарий 8: Общая проверка здоровья
```
Выбрать: 8 (Проверить здоровье системы)
Показывает:
  ✅ Использование диска
  ✅ Использование памяти
  ✅ Запущенные Python процессы
  ✅ Статус сервисов
  ✅ Последние ошибки Django
```

### ⚠️ Устранение неполадок:

#### Проблема: "ssh: command not found"
```powershell
# Установить OpenSSH
winget install Microsoft.OpenSSH.Beta

# Или через Settings:
# Settings → Apps → Optional Features → Add a feature → OpenSSH Client
```

#### Проблема: "Permission denied (publickey)"
```powershell
# Проверить SSH ключи
ssh-add -l

# Если пусто, добавить ключ
ssh-add $env:USERPROFILE\.ssh\id_rsa

# Скопировать ключ на сервер (заново)
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@72.56.81.163 "cat >> ~/.ssh/authorized_keys"
```

#### Проблема: "Connection timeout"
```powershell
# Проверить доступность сервера
Test-NetConnection -ComputerName 72.56.81.163 -Port 22

# Если не отвечает - проверить firewall на сервере
ssh root@72.56.81.163 "sudo ufw status"
ssh root@72.56.81.163 "sudo ufw allow 22/tcp"
```

### 📊 Мониторинг деплоя:

После деплоя проверьте:

1. **Сайт работает**: http://72.56.81.163
2. **API отвечает**: http://72.56.81.163/api/
3. **Админка доступна**: http://72.56.81.163/admin
4. **Нет ошибок в логах** (пункт 7 в меню)
5. **Все сервисы active** (пункт 6 в меню)

---

## 🎯 Вариант 1: Первая установка на сервер (deploy.sh)

### Что делает скрипт автоматически:

1. ✅ Устанавливает все зависимости (Python, PostgreSQL, Redis, Nginx)
2. ✅ Создаёт виртуальное окружение
3. ✅ Устанавливает Python пакеты
4. ✅ Настраивает базу данных и применяет миграции
5. ✅ Собирает статику Django
6. ✅ Билдит React фронтенд
7. ✅ Настраивает systemd сервисы (Gunicorn, Celery)
8. ✅ Настраивает Nginx с вашим доменом
9. ✅ Получает SSL сертификат (Let's Encrypt)
10. ✅ Запускает все сервисы

### 🎯 Пошаговая инструкция

#### Шаг 1: Подготовить .env файл (НА СЕРВЕРЕ)

```bash
# Скопировать .env.example
cp teaching_panel/.env.example teaching_panel/.env

# Отредактировать
nano teaching_panel/.env
```

**ОБЯЗАТЕЛЬНО изменить:**
```bash
SECRET_KEY=ваш-новый-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=ваш-домен.com,www.ваш-домен.com,72.56.81.163

# PostgreSQL
DATABASE_URL=postgresql://teaching_user:secure_password@localhost:5432/teaching_panel_db

# Email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password
DEFAULT_FROM_EMAIL=noreply@your-domain.com

# Домен
FRONTEND_URL=https://ваш-домен.com
SERVER_HOST=ваш-домен.com
```

#### Шаг 2: Скопировать проект на сервер

```bash
# НА ВАШЕМ КОМПЬЮТЕРЕ (из директории nat/)
scp -r teaching_panel frontend root@ваш-сервер-ip:/root/

# Или через Git:
# НА СЕРВЕРЕ
cd /root
git clone https://github.com/Syrnik1307/nat.git
cd nat
```

#### Шаг 3: Запустить автоматический деплой

```bash
# НА СЕРВЕРЕ
cd /root/teaching_panel/deployment  # или /root/nat/teaching_panel/deployment

# Сделать скрипт исполняемым
chmod +x deploy.sh

# ЗАПУСТИТЬ ДЕПЛОЙ
sudo bash deploy.sh
```

#### Шаг 4: Следовать подсказкам скрипта

Скрипт интерактивный, он спросит:

1. **Домен:** Введите ваш домен (например: `teachingpanel.ru`)
2. **Создать суперпользователя?** (y/n) - введите `y` чтобы создать админа
3. **Получить SSL сертификат?** (y/n) - введите `y` для HTTPS

## ✅ После успешного деплоя

### Проверить что всё работает:

1. **Открыть сайт:** `https://ваш-домен.com` или `http://72.56.81.163`
2. **Админка:** `https://ваш-домен.com/admin`
3. **API:** `https://ваш-домен.com/api/`

### Посмотреть логи:

```bash
# Django/Gunicorn логи
sudo journalctl -u teaching_panel -f

# Celery логи
sudo journalctl -u celery -f

# Nginx логи
sudo tail -f /var/log/nginx/teaching_panel_access.log
sudo tail -f /var/log/nginx/teaching_panel_error.log
```

### Управление сервисами:

```bash
# Перезапустить Django
sudo systemctl restart teaching_panel

# Перезапустить Celery
sudo systemctl restart celery celery-beat

# Перезапустить Nginx
sudo systemctl restart nginx

# Посмотреть статус всех сервисов
sudo systemctl status teaching_panel celery celery-beat nginx redis-server
```

---

## 📚 Дополнительная документация

- `DEPLOYMENT_GUIDE.md` - подробное руководство
- `PRE_DEPLOYMENT_CHECKLIST.md` - чеклист перед деплоем
- `PRODUCTION_CHECKLIST.md` - чеклист production настроек
- `.env.example` - пример переменных окружения
- `auto_deploy.ps1` - **новый улучшенный скрипт** ⭐
- `deploy_to_server.ps1` - старый интерактивный скрипт (deprecated)

## 🔗 Полезные команды

### На вашем компьютере (Windows):
```powershell
# Запустить автодеплой
.\auto_deploy.ps1

# Подключиться к серверу
ssh root@72.56.81.163

# Скопировать файл на сервер
scp .\local_file.txt root@72.56.81.163:/var/www/teaching_panel/
```

### На сервере (через SSH):
```bash
# Перезапустить Django
sudo systemctl restart teaching_panel

# Посмотреть логи Django
sudo journalctl -u teaching_panel -f

# Посмотреть ошибки Nginx
sudo tail -f /var/log/nginx/teaching_panel_error.log

# Применить миграции
cd /var/www/teaching_panel && source venv/bin/activate && python teaching_panel/manage.py migrate

# Собрать статику
cd /var/www/teaching_panel && source venv/bin/activate && python teaching_panel/manage.py collectstatic --noinput

# Пересобрать фронтенд
cd /var/www/teaching_panel/frontend && npm run build
```

---

## 🎯 Рекомендации

### Для ежедневной работы:
✅ Используйте **auto_deploy.ps1** - быстро, удобно, безопасно

### Для первой установки:
✅ Используйте **deploy.sh** на сервере - полная автоматизация

### Для экстренных ситуаций:
✅ Подключитесь через SSH и выполните команды вручную

---

**Последнее обновление**: 29 ноября 2025 г.
