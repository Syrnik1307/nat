# 📋 План реализации: Переработка системы "Группы и Ученики"

**Дата**: 4 декабря 2025 г.
**Статус**: Планирование
**Ответственный**: GitHub Copilot

---

## 📌 1. Обзор требований

### Проблема
В ЛК учителя на главной странице раздел "Ученики" содержит **всех учеников из всех групп**, что неправильно. Должны отображаться только **индивидуальные ученики** (не привязанные к группам).

### Основные требования

#### 1.1 Разделение учеников
- **Раздел "Мои группы"**: Список групп с кликом → модальное окно группы
- **Раздел "Ученики"**: Только индивидуальные ученики, не из групп
- Модальное окно группы содержит подразделы (табы)

#### 1.2 Модальное окно группы (структура)
При нажатии на группу открывается модаль с табами (для **групповых занятий**):

1. **Журнал посещений** (Attendance Log)
   - Таблица с колонками: Ученик | Занятие 1 | Занятие 2 | ... | Занятие N
   - Статусы: ✅ (был) | ❌ (не был) | 👁️ (посмотрел запись) | — (нет статуса)
   - Автоматическое заполнение при нажатии ученика на кнопку "Подключиться"
   - Возможность ручной корректировки учителем
   - **Функция**: Клик на ячейку → небольшой поп-ап с выбором статуса

2. **Тесты на проверку** (Tests to Review)
   - Список тестов (контрольных работ), отфильтрованные по группе
   - Разделение на: "На проверку" | "Проверены"
   - Статус каждого теста: количество ответов, кол-во проверенных
   - Клик → переход в редактор проверки

3. **Домашние задания** (Homework)
   - Список ДЗ группы
   - Разделение на: "Ожидают проверки" | "Проверены"
   - Статус: количество сданных, кол-во проверенных

4. **Контрольные точки** (Control Points)
   - Таблица контрольных точек группы
   - Аналогично журналу посещений
   - Ячейка = оценка или статус (не пройдена/пройдена)

5. **Рейтинг группы** (Group Rating)
   - Таблица рейтинга учеников в группе
   - Колонки: Место | Ученик | Очки | Посещение | ДЗ | Контрольные точки
   - Автоматический расчет: 
     - +X очков за посещение
     - -Y очков за отсутствие
     - +Z очков за выполненное ДЗ
     - +W очков за пройденные контрольные точки
   - Сортировка по очкам (убыванию)

6. **Отчеты** (Reports)
   - **Сводный отчет** группы
   - Статистика по посещениям (%)
   - Статистика по выполнению ДЗ (%)
   - Статистика по контрольным точкам (%)
   - График улучшения (если данные за период)
   - **Исключить**: Подключение к ВК, по темам (как на скрине 7)

#### 1.3 Карточка индивидуального ученика
При нажатии на ученика в разделе "Ученики" → **отдельная модаль** (не всплывающее окно) содержит:

- **Общая информация**: Имя, почта, статус подписки (если есть)
- **Краткий отчет**:
  - Всего часов на занятиях
  - Посещаемость (%)
  - Выполнено ДЗ / Всего ДЗ
  - Пройдено контрольных точек / Всего
  - Средний балл за контрольные точки
  - Рейтинг в группе (если ученик в группе)

- **Ошибки и пробелы** (если есть):
  - Список недовыполненных ДЗ
  - Список непройденных контрольных точек
  - Список пропущенных занятий

- **Замечания учителя**: Текстовое поле для вносимых преподавателем комментариев

---

## 📌 2. Логика рейтинга и система очков

### Система расчета очков

#### 2.1 Базовые значения (настраиваются в админ-панели)
- **Присутствие** (был на занятии): +10 очков
- **Просмотр записи** (посмотрел видео): +10 очков ✨ (суммируется с присутствием)
- **Отсутствие** (не подключился и не посмотрел запись): -5 очков (автоматически при отсутствии)
- **Выполненное ДЗ**: +5 очков
- **Пройденная контрольная точка**: +15 очков
- **Контрольная точка с ошибками**: +8 очков (50% от максимума)

#### 2.2 Где хранятся очки
- Модель `UserRating` (новая):
  - `user_id` (FK на User)
  - `group_id` (FK на Group или NULL для индивидуального)
  - `total_points` (общие очки)
  - `attendance_points` (очки за посещение)
  - `homework_points` (очки за ДЗ)
  - `control_points` (очки за контрольные точки)
  - `rank` (место в рейтинге группы, пересчитывается автоматически)
  - `updated_at`

#### 2.3 Логика автоматического заполнения посещений
1. **При подключении к занятию**: Ученик нажимает "Подключиться к Zoom" → статус автоматически = "✅ Был" (+10 очков)
2. **При просмотре записи**: Ученик смотрит запись занятия → статус = "👁️ Посмотрел" (+10 очков)
3. **При отсутствии**: Если ученик не подключился И не смотрел запись → статус автоматически = "❌ Не был" (-5 очков)
4. **Ручная корректировка**: Учитель может изменить статус вручную в любой момент

#### 2.4 Когда пересчитываются очки
- При подключении к занятию (автоматически +10)
- При просмотре записи (если еще не считали, +10)
- При отсутствии более 24 часов после окончания занятия (автоматически -5)
- При выполнении/проверке ДЗ (+5)
- При выполнении/проверке контрольной точки (+15 или +8)
- При ручном изменении статуса учителем
- Celery task для пересчета пропусков (ночной крон-джоб)

---

## 📌 3. Архитектура на фронте

### 3.1 Структура учеников

**Категория 1: Индивидуальные ученики**
- Могут быть БЕЗ группы (полностью индивидуальные занятия)
- Могут быть С группой (индивидуальные + групповые занятия)
- В разделе "Ученики" отображаются ВСЕ индивидуальные ученики (независимо от наличия группы)
- При клике → открывается карточка с личной статистикой (без рейтинга группы)

**Категория 2: Групповые ученики**
- Только в группах
- Отображаются ТОЛЬКО при открытии модали группы
- При клике в модали → карточка ученика (с информацией о посещениях и ДЗ в группе)

### 3.2 Компоненты (React)

#### Новые компоненты:
1. **`GroupDetailModal.js`** — Модаль группы с табами (групповые занятия)
2. **`StudentCardModal.js`** — Карточка индивидуального ученика (модаль)
3. **`AttendanceLog.js`** — Таб "Журнал посещений" (в модали группы)
4. **`GroupRating.js`** — Таб "Рейтинг группы" (в модали группы)
5. **`GroupReports.js`** — Таб "Отчеты" (в модали группы)
6. **`AttendanceStatusPicker.js`** — Мини-поп-ап для выбора статуса посещения
7. **`IndividualStudentsList.js`** — Список ТОЛЬКО индивидуальных учеников (обновить/создать)

#### Обновить компоненты:
- **`TeacherHomePage.js`** — Логика разделения учеников, обработка клика по группе
- **`TeacherHomePage.css`** — Стили модали и компонентов (в стиле системы)

### 3.2 API endpoint'ы (бэкенд)

#### Получение данных

```
GET /api/students/individual/
  → Список ТОЛЬКО индивидуальных учеников (без группы или с группой)

GET /api/groups/{id}/students/
  → Список учеников ГРУППЫ

GET /api/groups/{id}/attendance-log/
  → { lessons: [...], records: [...] }

GET /api/groups/{id}/rating/
  → { students: [...], stats: {...} }

GET /api/groups/{id}/reports/
  → { attendance: %, homework: %, control_points: %, ... }

GET /api/students/{id}/card/
  → { name, email, stats: {...}, errors: [...], notes: "..." }

GET /api/students/{id}/individual-card/
  → Карточка индивидуального ученика (без данных группы)
```

#### Обновление данных

```
POST /api/attendance-records/
  → { lesson_id, student_id, status: 'attended|absent|watched_recording' }

PUT /api/attendance-records/{id}/
  → { status: '...' }

PUT /api/students/{id}/teacher-notes/
  → { notes: "..." }
```

---

## 📌 4. Архитектура на бэкенде (Django)

### 4.1 Новые модели

```python
# accounts/models.py (добавить)

class StudentIndividual(models.Model):
    """Индивидуальный ученик (не в группе)"""
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    teacher = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
    teacher_notes = models.TextField(blank=True)
    created_at = models.DateTimeField(auto_now_add=True)

class UserRating(models.Model):
    """Рейтинг ученика в группе или общий"""
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    group = models.ForeignKey(Group, on_delete=models.CASCADE, null=True, blank=True)
    
    total_points = models.IntegerField(default=0)
    attendance_points = models.IntegerField(default=0)
    homework_points = models.IntegerField(default=0)
    control_points_value = models.IntegerField(default=0, db_column='control_points')
    
    rank = models.IntegerField(default=0)  # Место в рейтинге
    
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        unique_together = ('user', 'group')

class AttendanceRecord(models.Model):
    """Запись посещения"""
    STATUS_CHOICES = [
        ('attended', 'Был'),
        ('absent', 'Не был'),
        ('watched_recording', 'Посмотрел запись'),
    ]
    
    lesson = models.ForeignKey(Lesson, on_delete=models.CASCADE)
    student = models.ForeignKey(User, on_delete=models.CASCADE)
    
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, null=True, blank=True)
    auto_recorded = models.BooleanField(default=False)  # Был ли заполнен автоматически
    recorded_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True, blank=True)  # Кто заполнил (учитель)
    
    recorded_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

### 4.2 Сервисы (services)

```python
# schedule/attendance_service.py (новый)

class AttendanceService:
    """Сервис управления посещениями"""
    
    @staticmethod
    def auto_record_attendance(lesson_id, student_id, is_joined=True):
        """Автоматически записать посещение"""
        # Логика: если ученик нажал кнопку "Подключиться", то status='attended'
        
    @staticmethod
    def manual_record_attendance(lesson_id, student_id, status, teacher_id):
        """Ручное заполнение учителем"""
        
    @staticmethod
    def recalculate_rating(user_id, group_id=None):
        """Пересчитать рейтинг и очки"""
        # Суммирование всех очков из attendance, homework, control_points
        # Пересчет rank в группе
```

### 4.3 ViewSet'ы (Views)

```python
# schedule/views.py (добавить или обновить)

class AttendanceRecordViewSet(viewsets.ModelViewSet):
    queryset = AttendanceRecord.objects.all()
    serializer_class = AttendanceRecordSerializer
    permission_classes = [IsAuthenticated]
    
    @action(detail=False, methods=['get'])
    def group_attendance_log(self, request, group_id=None):
        """GET /api/attendance-records/group_attendance_log/?group_id=1"""
        
    @action(detail=False, methods=['post'])
    def auto_record(self, request):
        """POST /api/attendance-records/auto_record/"""
        # { lesson_id, student_id, is_joined: bool }

class GroupRatingViewSet(viewsets.ViewSet):
    """Рейтинг группы"""
    
    @action(detail=True, methods=['get'])
    def rating(self, request, pk=None):
        """GET /api/groups/{id}/rating/"""
        # Возвращает отсортированный список учеников с очками

class StudentCardViewSet(viewsets.ViewSet):
    """Карточка ученика"""
    
    @action(detail=True, methods=['get'])
    def card(self, request, pk=None):
        """GET /api/students/{id}/card/"""
        # Краткий отчет по ученику
```

---

## 📌 5. UI/UX Дизайн

### 5.1 Стиль системы (текущий)

**Цвета** (из TeacherHomePage.css):
- Основной синий: `#2563eb`
- Темный синий: `#1e40af`
- Зеленый (успех): `#10b981`
- Красный (ошибка): `#ef4444`
- Оранжевый (предупреждение): `#f59e0b`
- Серый фон: `#f3f4f6`
- Серый текст: `#6b7280`
- Граница: `#e5e7eb`

**Компоненты**:
- Кнопки: Скругленные, с теневым эффектом
- Модали: `border-radius: 16px`, тень, белый фон
- Таблицы: Линии, полосы по цветам
- Статусы: Иконки (✅, ❌, 👁️, —)

### 5.2 Макет модального окна группы

```
┌──────────────────────────────────────────┐
│  Мой класс — 10А (15 учеников)      [✕]  │
├──────────────────────────────────────────┤
│  [Журнал][Тесты][ДЗ][Контроль][Рейтинг] │
├──────────────────────────────────────────┤
│                                          │
│  Содержимое активного таба               │
│                                          │
│                                          │
└──────────────────────────────────────────┘
```

### 5.3 Журнал посещений (скрин 2)

```
┌────────────┬─────────────┬─────────────┬─────┐
│ Ученик     │ Занятие 1   │ Занятие 2   │ ... │
├────────────┼─────────────┼─────────────┼─────┤
│ Иван       │  ✅         │  ❌         │     │
│ Мария      │  ✅         │  ✅         │     │
│ Петр       │  👁️         │  ✅         │     │
└────────────┴─────────────┴─────────────┴─────┘

Клик на ячейку → мини-поп-ап:
┌────────────────────┐
│  ✅ Был            │
│  ❌ Не был         │
│  👁️ Посмотрел      │
│  — Нет статуса     │
└────────────────────┘
```

---

## 📌 6. Логика тестирования на проде

### 6.1 Как тестировать
1. **Локальная разработка**: Отключить, работаем сразу на проде
2. **Проде**: API запросы тестируются через curl/Postman
3. **Фронт**: Проверяются компоненты на работающем сервере

### 6.2 Данные для тестирования
- Учитель: test-teacher@example.com
- Группа: "10А класс" (создать, если нет)
- Ученики: min 5 шт (mix индивидуальные + в группе)
- Занятия: min 3 шт для группы

### 6.3 Чек-лист тестирования
- [ ] Ученики в разделе "Ученики" — только индивидуальные
- [ ] Клик на группу → модаль открывается
- [ ] Табы модали работают (переключение)
- [ ] Журнал посещений: клик на ячейку → выбор статуса
- [ ] Рейтинг: очки пересчитываются (+ добавить запись, - удалить)
- [ ] Карточка ученика: данные корректные
- [ ] Стили соответствуют системе (цвета, шрифты, отступы)

---

## 📌 7. План реализации (этапы)

### Этап 1: Backend (2-3 дня)
1. Создать модели (UserRating, AttendanceRecord, StudentIndividual)
2. Создать сервис AttendanceService
3. Создать ViewSet'ы и endpoint'ы
4. Миграции, тестирование API

### Этап 2: Frontend Components (2-3 дня)
1. Создать GroupDetailModal с табами
2. Создать компоненты для каждого таба
3. Интегрировать с TeacherHomePage
4. Стилизация (CSS)

### Этап 3: Интеграция + Тестирование (1-2 дня)
1. Связать фронт и бэкенд
2. Полное тестирование на проде
3. Доработки по результатам тестирования

### Этап 4: Деплой (1 день)
1. Мануальный деплой на продакшн
2. Проверка в production
3. Документирование изменений

---

## 📌 8. Файлы для создания/обновления

### Backend:
- `teaching_panel/accounts/models.py` — Модели (UserRating, AttendanceRecord)
- `teaching_panel/schedule/attendance_service.py` — Сервис посещений
- `teaching_panel/schedule/views.py` — ViewSet'ы (обновить)
- `teaching_panel/schedule/serializers.py` — Сериализаторы (новые)
- `teaching_panel/schedule/urls.py` — Routes (обновить)
- Миграции (автоматически)

### Frontend:
- `frontend/src/components/GroupDetailModal.js` — Модаль группы (новый)
- `frontend/src/components/StudentCardModal.js` — Карточка ученика (новый)
- `frontend/src/components/tabs/AttendanceLog.js` — Таб посещений (новый)
- `frontend/src/components/tabs/GroupRating.js` — Таб рейтинга (новый)
- `frontend/src/components/tabs/GroupReports.js` — Таб отчетов (новый)
- `frontend/src/components/TeacherHomePage.js` — Обновить логику
- `frontend/src/components/TeacherHomePage.css` — Добавить стили для модалей
- `frontend/src/apiService.js` — Новые методы для API

---

## 📌 9. Следующие шаги

1. **Уточнение требований** ✅ Завершено
2. **Старт разработки** (Этап 1: Backend)
3. **Регулярное тестирование** на проде

---

**Статус**: ✅ Утверждено, готово к разработке

**Дополнения по уточнению**:
- ✅ Просмотр записи: +10 очков (суммируется с присутствием)
- ✅ Отсутствие: автоматически -5 очков
- ✅ Карточка ученика: отдельная модаль
- ✅ Индивидуальные ученики: отдельная категория (могут быть в группе)
- ✅ Рейтинг индивидуального ученика: личная статистика (те же метрики)
