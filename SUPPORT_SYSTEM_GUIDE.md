# 🎫 Руководство по системе поддержки

## Что реализовано

Полноценная система обращений в поддержку с **Telegram-интеграцией**:

✅ Виджет поддержки на всех страницах платформы  
✅ Создание тикетов с приоритетами и категориями  
✅ Чат с поддержкой в реальном времени  
✅ **Telegram-бот для админов с двусторонней связью**  
✅ **Уведомления о новых тикетах и сообщениях в Telegram**  
✅ **Возможность отвечать прямо из Telegram**  
✅ Автоматическое обновление статусов  

## 🚀 Быстрый старт Telegram-интеграции

**Полное руководство:** См. файл `TELEGRAM_SUPPORT_SETUP.md`

**Краткая версия:**

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Установите переменную: `$env:SUPPORT_BOT_TOKEN="ваш_токен"`
3. Запустите бота: `python support_bot.py`
4. Добавьте Telegram ID в Django Admin (поле `telegram_id`)
5. Напишите боту `/start` в Telegram

**Готово!** Теперь вы получаете уведомления и можете отвечать из Telegram.

## Для пользователей

### Как создать обращение

1. **Найдите плавающую кнопку** внизу справа (фиолетовый круг со знаком вопроса)
2. **Нажмите** на неё — откроется виджет поддержки
3. **Заполните форму**:
   - Тема обращения
   - Подробное описание проблемы
   - Категория (техническая, аккаунт, функция, другое)
   - Приоритет (низкий, обычный, высокий, срочный)
4. **Отправьте** — тикет создан!

### Переписка с поддержкой

- После создания тикета вы можете **писать сообщения** в чате
- Ответы сотрудников поддержки **выделены другим цветом**
- **Непрочитанные сообщения** отображаются красным значком на кнопке
- Система автоматически **обновляет счётчик** каждые 30 секунд

### Статусы тикетов

- 🟢 **Новый** — тикет только что создан
- 🔵 **В работе** — поддержка работает над проблемой
- 🟡 **Ожидает ответа** — поддержка ждёт вашего ответа
- ⚫ **Решён** — проблема решена
- ⚫ **Закрыт** — тикет закрыт

## Для администраторов

### Управление через Django Admin

Откройте `/admin/support/`:

#### SupportTicket (Тикеты)
- Просмотр всех обращений
- Фильтрация по статусу, приоритету, категории
- Назначение тикетов сотрудникам
- Изменение статуса

#### SupportMessage (Сообщения)
- Просмотр всей переписки
- Ответы пользователям
- Отметка сообщений как прочитанных

#### QuickSupportResponse (Быстрые ответы)
- Создание шаблонов ответов
- Категоризация по типам проблем
- Статистика использования

### API для работы с тикетами

#### Получить список тикетов
```
GET /api/support/tickets/
```

Пользователи видят только свои тикеты, сотрудники — все.

#### Создать тикет
```
POST /api/support/tickets/
{
  "subject": "Не работает вход",
  "description": "Подробное описание проблемы",
  "category": "technical",
  "priority": "high",
  "email": "user@example.com",  // опционально для анонимов
  "name": "Иван Иванов"  // опционально
}
```

#### Отправить сообщение
```
POST /api/support/tickets/{id}/add_message/
{
  "message": "Текст сообщения"
}
```

#### Отметить как прочитанное
```
POST /api/support/tickets/{id}/mark_read/
{
  "message_ids": [1, 2, 3]
}
```

#### Решить тикет
```
POST /api/support/tickets/{id}/resolve/
```

#### Снова открыть
```
POST /api/support/tickets/{id}/reopen/
```

#### Получить непрочитанные
```
GET /api/support/unread-count/
```

#### Быстрые ответы (только для staff)
```
GET /api/support/quick-responses/
```

## Технические детали

### Структура файлов

```
teaching_panel/
  support/
    __init__.py
    admin.py           # Админ-панель
    apps.py
    models.py          # Модели: SupportTicket, SupportMessage, QuickSupportResponse
    serializers.py     # Сериализаторы DRF
    views.py           # API ViewSet с кастомными действиями
    urls.py            # Маршруты API
    migrations/
      0001_initial.py  # Миграция базы данных

frontend/src/components/
  SupportWidget.js     # React-компонент виджета
  SupportWidget.css    # Стили виджета
```

### Интеграция в страницы

Виджет уже добавлен в:
- `TeacherHomePage.js` — главная страница учителя
- `StudentHomePage.js` — главная страница ученика

### Модели базы данных

**SupportTicket**:
- `user` — пользователь (может быть null для анонимных)
- `email` / `name` — для анонимных обращений
- `subject` / `description` — тема и описание
- `status` — статус тикета
- `priority` — приоритет
- `category` — категория проблемы
- `assigned_to` — кому назначен
- `page_url` / `user_agent` / `screenshot` — техническая информация
- `created_at` / `updated_at` / `resolved_at` — временные метки

**SupportMessage**:
- `ticket` — ссылка на тикет
- `author` — автор (может быть null для системных)
- `message` — текст сообщения
- `is_staff_reply` — ответ сотрудника или нет
- `attachments` — JSON с прикреплёнными файлами
- `read_by_user` / `read_by_staff` — статусы прочтения

**QuickSupportResponse**:
- `title` / `message` — заголовок и текст шаблона
- `category` — категория
- `usage_count` — счётчик использований
- `is_active` — активен или нет

### Функции безопасности

- **Авторизация**: IsAuthenticated для большинства операций
- **Изоляция данных**: пользователи видят только свои тикеты
- **Анонимная поддержка**: возможность создания тикетов без авторизации
- **Валидация**: все входные данные проверяются на уровне сериализаторов
- **CSRF**: защита от CSRF через Django

### Планируемые улучшения

1. ~~**Уведомления в Telegram**~~ ✅ **РЕАЛИЗОВАНО!**
2. **Загрузка файлов** — возможность прикрепить скриншоты к обращению
3. **Email-уведомления** — опционально для критических обращений
4. **Поиск** — поиск по тикетам и сообщениям
5. **Статистика** — дашборд с метриками работы поддержки
6. **SLA-правила** — автоматические эскалации при просрочке
7. **Теги** — дополнительная категоризация тикетов

## 📱 Telegram-интеграция

### Возможности

- **Уведомления:** Админы получают сообщения о новых тикетах и ответах пользователей
- **Ответы из Telegram:** Можно отвечать на тикеты не заходя на платформу
- **Управление тикетами:** Назначение, смена статуса, просмотр - всё в Telegram
- **Быстрые ответы:** После просмотра тикета можно просто написать сообщение

### Команды бота

- `/start` - Регистрация админа
- `/tickets` - Список открытых тикетов
- `/my` - Мои назначенные тикеты
- `/view_<id>` - Просмотр тикета (например, `/view_5`)
- `/reply <id> <текст>` - Ответ на тикет
- `/stats` - Статистика поддержки
- `/help` - Справка

### Настройка

**Подробная инструкция:** См. `TELEGRAM_SUPPORT_SETUP.md`

1. Создайте бота через @BotFather
2. Установите токен: `$env:SUPPORT_BOT_TOKEN="токен"`
3. Запустите бота: `python support_bot.py`
4. Привяжите Telegram ID в Django Admin
5. Готово!

## Быстрый старт

### 1. Запустите миграции (уже применены)
```bash
cd teaching_panel
python manage.py migrate support
```

### 2. Создайте быстрые ответы (опционально)
Через Django Admin → Support → Quick support responses

### 3. Запустите серверы
```bash
# Backend
cd teaching_panel
python manage.py runserver

# Frontend
cd frontend
npm start
```

### 4. Протестируйте
1. Откройте http://localhost:3000/home-new
2. Нажмите на кнопку поддержки внизу справа
3. Создайте тестовый тикет
4. Проверьте в Django Admin → Support → Tickets

## Поддержка

Если у вас возникли вопросы по системе поддержки:
- Проверьте логи Django: `python manage.py runserver`
- Проверьте консоль браузера на ошибки
- Проверьте что миграции применены: `python manage.py showmigrations support`

Система готова к использованию! 🚀
