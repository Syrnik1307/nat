# 🤖 Lectio Concierge — AI-Powered Support System

## Обзор

Lectio Concierge — это гибридная система поддержки, которая:

1. **AI-агент** отвечает на типовые вопросы автоматически (RAG по Knowledge Base)
2. **Автоматические действия** диагностируют проблемы (проверка Zoom, подписки, записей)
3. **Эскалация к человеку** через Telegram, если AI не справляется
4. **Real-time синхронизация** между Web-виджетом и Telegram

## Статус: 🚧 В разработке

Модуль готов к тестированию на staging. Не включён в production.

---

## 📁 Структура модуля

```
concierge/
├── models.py              # Conversation, Message, KnowledgeDocument, ActionDefinition
├── views.py               # REST API endpoints
├── serializers.py         # DRF serializers
├── urls.py                # URL routing
├── admin.py               # Django Admin
├── apps.py                # App config
│
├── services/
│   ├── conversation_service.py  # Главный pipeline обработки сообщений
│   ├── ai_service.py            # RAG + LLM логика
│   ├── knowledge_service.py     # Индексация и поиск по Knowledge Base
│   ├── telegram_bridge.py       # Синхронизация с Telegram
│   └── action_executor.py       # Выполнение автоматических действий
│
├── actions/                     # Автоматические действия (read-only)
│   ├── check_zoom.py
│   ├── check_subscription.py
│   ├── check_recordings.py
│   ├── check_system_status.py
│   └── check_lesson.py
│
├── fixtures/
│   └── initial_actions.json    # Начальные данные для ActionDefinition
│
└── migrations/
```

---

## 🚀 Установка и запуск

### 1. Добавить в INSTALLED_APPS

```python
# teaching_panel/settings.py

INSTALLED_APPS = [
    ...
    'concierge',  # Добавить
]
```

### 2. Добавить URL

```python
# teaching_panel/urls.py

urlpatterns = [
    ...
    path('api/concierge/', include('concierge.urls')),
]
```

### 3. Создать миграции и применить

```bash
cd teaching_panel
python manage.py makemigrations concierge
python manage.py migrate
```

### 4. Загрузить начальные данные

```bash
python manage.py loaddata concierge/fixtures/initial_actions.json
```

### 5. Индексировать Knowledge Base

```bash
python manage.py shell -c "
from concierge.services.knowledge_service import KnowledgeService
import asyncio
print(asyncio.run(KnowledgeService.index_all_documents()))
"
```

### 6. Настроить переменные окружения

```bash
# AI Provider (выбрать один)
DEEPSEEK_API_KEY=sk-...
# или
OPENAI_API_KEY=sk-...

# Опционально: выбор провайдера
CONCIERGE_AI_PROVIDER=deepseek  # или openai

# Telegram (использует существующие)
SUPPORT_BOT_TOKEN=...
SUPPORT_NOTIFICATIONS_CHAT_ID=...

# Опционально: webhook secret
CONCIERGE_WEBHOOK_SECRET=random-secret-string
```

---

## 📡 API Endpoints

### Диалоги

| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | `/api/concierge/conversations/` | Создать диалог |
| GET | `/api/concierge/conversations/` | Список диалогов (свои) |
| GET | `/api/concierge/conversations/{id}/` | Детали диалога |
| POST | `/api/concierge/conversations/{id}/messages/` | Отправить сообщение |
| GET | `/api/concierge/conversations/{id}/messages/` | История сообщений |
| POST | `/api/concierge/conversations/{id}/rate/` | Оценить диалог |
| POST | `/api/concierge/conversations/{id}/close/` | Закрыть диалог |
| POST | `/api/concierge/conversations/{id}/escalate/` | Позвать оператора |

### Real-time

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/concierge/conversations/{id}/stream/` | SSE поток сообщений |

### Действия

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/concierge/conversations/{id}/actions/` | Доступные действия |
| POST | `/api/concierge/conversations/{id}/actions/{name}/execute/` | Выполнить действие |

### Telegram Webhook

| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | `/api/concierge/telegram/webhook/` | Webhook для Telegram |

### Admin

| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | `/api/concierge/knowledge/reindex/` | Переиндексировать KB |

---

## 🧠 Pipeline обработки сообщения

```
┌─────────────────────────────────────────────────────────────────┐
│  Пользователь отправляет сообщение                              │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. Сохранение в БД (Message)                                   │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. Проверка статуса диалога                                    │
│     - AI_MODE → продолжаем                                      │
│     - HUMAN_MODE → форвард в Telegram                           │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. RAG Search (Knowledge Base)                                 │
│     - Поиск релевантных чанков                                  │
│     - Формирование контекста                                    │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. LLM Decision (DeepSeek/OpenAI)                              │
│     - answer: ответить сразу                                    │
│     - clarify: уточнить (макс. 2 раза)                          │
│     - action: выполнить автоматическое действие                 │
│     - escalate: передать оператору                              │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. Обработка решения                                           │
│     - answer → сохранить и вернуть                              │
│     - clarify → сохранить, инкремент retry_count                │
│     - action → выполнить, сформировать ответ                    │
│     - escalate → создать топик в Telegram, уведомить            │
└───────────────────────────────┬─────────────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│  6. Ответ пользователю (через API или SSE)                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📚 Knowledge Base

Документы для AI хранятся в `docs/knowledge/`.

**Подробная документация:** [docs/knowledge/README.md](../docs/knowledge/README.md)

### Структура:

```
docs/knowledge/
├── faq/               # FAQ по темам
├── guides/            # Пошаговые инструкции
├── troubleshooting/   # Решение проблем
└── README.md          # Руководство по созданию контента
```

### Индексация:

```python
# Вручную
from concierge.services.knowledge_service import KnowledgeService
import asyncio
asyncio.run(KnowledgeService.index_all_documents())

# Через API (только админ)
POST /api/concierge/knowledge/reindex/
```

---

## 🔧 Автоматические действия

| Имя | Описание | Read-only |
|-----|----------|-----------|
| `check_zoom_status` | Статус Zoom аккаунтов | ✅ |
| `check_subscription` | Статус подписки пользователя | ✅ |
| `check_recordings` | Статус записей уроков | ✅ |
| `check_system_status` | Общий статус системы | ✅ |
| `check_lesson` | Статус конкретного урока | ✅ |

### Добавление нового действия:

1. Создать файл в `concierge/actions/`:

```python
# concierge/actions/my_action.py

async def execute(params: dict) -> dict:
    user = params.get('_user')
    conversation = params.get('_conversation')
    
    # Логика действия
    
    return {
        'success': True,
        'message': 'Результат для пользователя',
        'data': {'key': 'value'},
    }
```

2. Добавить в БД через admin или fixture:

```json
{
    "name": "my_action",
    "display_name": "Моё действие",
    "handler_path": "concierge.actions.my_action",
    "trigger_keywords": ["ключевые", "слова"],
    "is_read_only": true,
    "is_active": true
}
```

---

## 📱 Telegram интеграция

### Как это работает:

1. AI эскалирует диалог → создаётся топик в группе поддержки
2. Админы видят информацию о пользователе и диалог
3. Админ отвечает в топике → сообщение попадает в web-чат
4. Пользователь видит ответ в реальном времени (SSE)

### Настройка Webhook:

```bash
# Установить webhook (на production)
curl "https://api.telegram.org/bot{TOKEN}/setWebhook?url=https://yourdomain.com/api/concierge/telegram/webhook/&secret_token={SECRET}"
```

---

## 🧪 Тестирование

```python
# Создать диалог
POST /api/concierge/conversations/
{
    "page_url": "https://app.lectio.ru/payments",
    "page_title": "Оплата"
}

# Отправить сообщение
POST /api/concierge/conversations/1/messages/
{
    "content": "Как оплатить подписку?"
}

# Проверить ответ AI
GET /api/concierge/conversations/1/
```

---

## 📊 Мониторинг

### Django Admin:

- `/admin/concierge/conversation/` — все диалоги
- `/admin/concierge/message/` — все сообщения
- `/admin/concierge/actionexecution/` — лог действий

### Метрики:

- `ConversationMetrics` — агрегированная статистика по дням
- `ai_only_resolved` — сколько диалогов решено без человека
- `escalated_to_human` — сколько передано операторам

---

## 🔮 TODO (следующие фазы)

- [ ] **Phase 2:** pgvector для embedding-based поиска
- [ ] **Phase 2:** Больше автоматических действий
- [ ] **Phase 3:** WebSocket вместо SSE
- [ ] **Phase 3:** Typing indicators
- [ ] **Phase 3:** Голосовые сообщения
- [ ] **Phase 3:** Аналитика и A/B тесты промптов

---

## 👥 Контакты

По вопросам разработки — пиши в чат проекта.
